import { render, screen } from '@testing-library/react';
// import Gallery from '../Gallery';

describe('Gallery Page', () => {
  it('renders the gallery page title', () => {
    // render(<Gallery />);
    // expect(screen.getByText(/Nail Art Gallery/i)).toBeInTheDocument();
  });

  it('shows empty state when no exhibits are found', () => {
    // TODO: Implement test
  });
});
