---
phase: 06-missing-features
plan: 02
type: execute
wave: 1
depends_on: [00]
files_modified:
  - backend/src/utils/systemLog.ts
  - backend/src/controllers/staffController.ts
  - backend/src/controllers/appointmentCompletion.ts
  - backend/src/controllers/payrollController.ts
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - System retains an immutable history of sensitive actions for compliance
    - Users can complete sensitive business operations even if the audit log system experiences temporary unavailability
  artifacts:
    - path: backend/src/utils/systemLog.ts
      provides: Reusable audit logging utility
      min_lines: 20
    - path: backend/src/controllers/payrollController.ts
      provides: Audited payroll actions
      min_lines: 150
  key_links:
    - from: backend/src/controllers/staffController.ts
      to: backend/src/utils/systemLog.ts
      via: Function call to logSystemAction
    - from: backend/src/controllers/appointmentCompletion.ts
      to: backend/src/utils/systemLog.ts
      via: Function call to logSystemAction
    - from: backend/src/controllers/payrollController.ts
      to: backend/src/utils/systemLog.ts
      via: Function call to logSystemAction
---

<objective>
Implement the system audit trail utility and integrate it across sensitive controller endpoints.
Purpose: Comply with security and operational tracking requirements for critical system actions.
Output: A resilient logging utility and updated controllers that emit audit events.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-missing-features/06-PATTERNS.md
</execution_context>

<context>
@backend/src/utils/systemLog.ts
@backend/src/controllers/staffController.ts
@backend/src/controllers/appointmentCompletion.ts
@backend/src/controllers/payrollController.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Audit Logger Utility</name>
  <files>backend/src/utils/systemLog.ts</files>
  <read_first>
    - backend/prisma/schema.prisma
    - backend/src/middleware/authMiddleware.ts
  </read_first>
  <action>
    Create `backend/src/utils/systemLog.ts` and implement the `logSystemAction` utility.
    Reference the Utility method pattern and Auth Context Extraction from 06-PATTERNS.md (analog `backend/src/utils/apiHelpers.ts`).
    The signature should be: `export const logSystemAction = async (req: AuthRequest, action: string, entityType?: string, entityId?: number, details?: Record<string, any>)`
    Ensure to import `AuthRequest` from `../middleware/authMiddleware` or the correct path.
    Inside the function:
    1. Use a `try/catch` block. On catch, use `console.error` to log the failure, but DO NOT rethrow the error.
    2. Extract `userId` from `req.user?.sub`.
    3. Extract `ipAddress` from `req.ip || req.socket.remoteAddress`.
    4. Extract `userAgent` from `req.headers['user-agent']`.
    5. Call `prisma.systemLog.create` to insert the record.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/systemLog.test.ts first</automated>
  </verify>
  <done>
    - `backend/src/utils/systemLog.ts` exports `logSystemAction`
    - `backend/src/utils/systemLog.ts` contains `try` and `catch` block wrapping the database insertion
    - `backend/src/utils/systemLog.ts` extracts `req.ip` and `req.headers['user-agent']`
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Audit Logging to Staff and Appointments</name>
  <files>backend/src/controllers/staffController.ts, backend/src/controllers/appointmentCompletion.ts</files>
  <read_first>
    - backend/src/controllers/staffController.ts
    - backend/src/controllers/appointmentCompletion.ts
    - backend/src/utils/systemLog.ts
  </read_first>
  <action>
    Import `logSystemAction` into both controllers.
    Reference the Try/Catch with Response Pattern and Prisma Transaction Pattern from 06-PATTERNS.md for the respective files.
    In `backend/src/controllers/staffController.ts`:
    - In `createStaff`: call `await logSystemAction(req as AuthRequest, 'STAFF_CREATED', 'Staff', newStaff.id, { message: 'Created staff profile' })` after successful creation.
    - In `updateStaff`: call `await logSystemAction(req as AuthRequest, 'STAFF_UPDATED', 'Staff', Number(id), { message: 'Updated staff profile' })` after successful update.
    - In `updateStaffSchedule`: call `await logSystemAction(req as AuthRequest, 'SCHEDULE_UPDATED', 'Staff', Number(id), { message: 'Updated staff schedule' })` after successful update.
    
    In `backend/src/controllers/appointmentCompletion.ts`:
    - In the appointment completion logic, call `await logSystemAction(req as AuthRequest, 'COMMISSIONS_CREATED', 'Appointment', Number(id), { message: 'Calculated commissions for appointment' })` after commissions are created and transaction completes.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/staffController.test.ts first</automated>
  </verify>
  <done>
    - `backend/src/controllers/staffController.ts` contains `import { logSystemAction }`
    - `backend/src/controllers/staffController.ts` contains `logSystemAction`
    - `backend/src/controllers/appointmentCompletion.ts` contains `logSystemAction`
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Audit Logging to Payroll</name>
  <files>backend/src/controllers/payrollController.ts</files>
  <read_first>
    - backend/src/controllers/payrollController.ts
    - backend/src/utils/systemLog.ts
  </read_first>
  <action>
    Import `logSystemAction` in `backend/src/controllers/payrollController.ts`.
    Reference the Imports pattern and Error handling pattern from 06-PATTERNS.md for `payrollController.ts`.
    - In `generatePayroll`: call `await logSystemAction(req as AuthRequest, 'PAYROLL_GENERATED', 'PayrollPeriod', Number(periodId), { message: 'Generated payroll for period' })` after successful generation.
    - In `lockPayroll`: call `await logSystemAction(req as AuthRequest, 'PAYROLL_LOCKED', 'PayrollPeriod', Number(id), { message: 'Locked payroll period' })` after successful lock.
    - In `addDeduction`: call `await logSystemAction(req as AuthRequest, 'DEDUCTION_ADDED', 'Deduction', newDeduction.id, { message: 'Added payroll deduction' })` after successful creation.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/payrollController.test.ts first</automated>
  </verify>
  <done>
    - `backend/src/controllers/payrollController.ts` contains `import { logSystemAction }`
    - `backend/src/controllers/payrollController.ts` contains `logSystemAction`
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Database | Writing logs must not impact system availability if logging fails. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-1 | Denial of Service | systemLog.ts | mitigate | Wrap logging in try/catch to fail silently and avoid crashing the main transaction. |
| T-06-02-2 | Information Disclosure | systemLog.ts | mitigate | Only log IDs and generic actions, avoid logging PII or payload diffs. |
</threat_model>

<verification>
- `logSystemAction` utility is correctly implemented and resilient against failure.
- Controller endpoints emit the specified event strings.
</verification>

<success_criteria>
- Sensitive actions correctly generate SystemLog records without crashing existing logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-missing-features/06-02-SUMMARY.md`
</output>