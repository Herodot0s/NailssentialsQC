---
phase: 06-missing-features
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/controllers/reportController.ts
  - backend/prisma/seed.ts
autonomous: true
requirements:
  - FEAT-03
must_haves:
  truths:
    - Managers can configure the global sales target dynamically
    - System retains configuration across restarts without relying on hardcoded values
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: SystemSettings model and PayrollPeriod.sales_target
      min_lines: 300
    - path: backend/src/controllers/reportController.ts
      provides: Target calculation fallback logic
      min_lines: 100
    - path: backend/prisma/seed.ts
      provides: Initial seed of global_sales_target
      min_lines: 50
  key_links:
    - from: backend/src/controllers/reportController.ts
      to: backend/prisma/schema.prisma
      via: Prisma query to SystemSettings and PayrollPeriod
---

<objective>
Implement database-driven configurable sales targets.
Purpose: Replace the hardcoded 8000 value with a flexible system setting and period-specific overrides.
Output: Updated Prisma schema and report controller logic.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-missing-features/06-PATTERNS.md
</execution_context>

<context>
@backend/prisma/schema.prisma
@backend/src/controllers/reportController.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update Prisma Schema</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma
  </read_first>
  <action>
    Modify `backend/prisma/schema.prisma`:
    Reference the model definition pattern from 06-PATTERNS.md for the closest analog (`backend/prisma/schema.prisma`).
    1. Add a new `SystemSettings` model exactly as follows:
       ```prisma
       model SystemSettings {
         id          Int      @id @default(autoincrement())
         key         String   @unique
         value       String
         description String?
       }
       ```
    2. Add `sales_target Decimal?` to the existing `PayrollPeriod` model.
  </action>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>
    - `backend/prisma/schema.prisma` contains `model SystemSettings`
    - `backend/prisma/schema.prisma` contains `sales_target` in `PayrollPeriod`
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migrate Database Schema</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma
  </read_first>
  <action>
    Run the schema migration command inside the `backend` directory to apply the new `SystemSettings` model and `PayrollPeriod` fields to the live database, safely versioning the schema changes.
    Run: `cd backend && npx prisma migrate dev --name add_system_settings`
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>
    - Command `cd backend && npx prisma migrate dev --name add_system_settings` completes with exit code 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Target Fallback in Report Controller</name>
  <files>backend/src/controllers/reportController.ts</files>
  <read_first>
    - backend/src/controllers/reportController.ts
    - backend/prisma/schema.prisma
  </read_first>
  <action>
    Update `backend/src/controllers/reportController.ts` to implement D-08 and D-09 fallback logic for the sales target, replacing any hardcoded `8000` or `8000.00` values.
    Reference the Aggregate Data Pattern from 06-PATTERNS.md for `reportController.ts`.
    
    1. Query the currently active `PayrollPeriod` (where `is_locked` is false or similar active condition).
    2. If an active period exists and `sales_target` is not null, use it.
    3. Otherwise, query `prisma.systemSettings.findUnique({ where: { key: 'global_sales_target' } })`.
    4. If the setting exists, parse its `value` to a number/Decimal.
    5. If neither exists, fallback to `8000.00`.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/reportController.test.ts first</automated>
  </verify>
  <done>
    - `backend/src/controllers/reportController.ts` contains `global_sales_target`
    - `backend/src/controllers/reportController.ts` contains a fallback default `8000`
  </done>
</task>

<task type="auto">
  <name>Task 4: Seed Initial Sales Target</name>
  <files>backend/prisma/seed.ts</files>
  <read_first>
    - backend/prisma/seed.ts
  </read_first>
  <action>
    Update `backend/prisma/seed.ts` to insert the initial `global_sales_target` value into `SystemSettings`.
    Add the following Prisma query inside the main seed function:
    ```typescript
    await prisma.systemSettings.upsert({
      where: { key: 'global_sales_target' },
      update: {},
      create: {
        key: 'global_sales_target',
        value: '8000.00',
        description: 'Global monthly sales target fallback'
      }
    });
    ```
    Then run `cd backend && npx prisma db seed` to apply the seed.
  </action>
  <verify>
    <automated>cd backend && npx prisma db seed</automated>
  </verify>
  <done>
    - `backend/prisma/seed.ts` contains `key: 'global_sales_target'`
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Database -> API | Trust that values retrieved from SystemSettings are safe, but handle missing keys gracefully. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-1 | Denial of Service | reportController.ts | mitigate | Ensure fallback to 8000 exists if DB lookup fails or is null. |
</threat_model>

<verification>
- `npx prisma migrate dev` has successfully applied the schema changes.
- The report controller correctly falls back to 8000 when DB values are absent.
</verification>

<success_criteria>
- Schema contains the SystemSettings table.
- Report controller incorporates dynamic DB-driven target logic instead of static 8000.
</success_criteria>

<output>
After completion, create `.planning/phases/06-missing-features/06-01-SUMMARY.md`
</output>