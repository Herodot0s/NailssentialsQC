---
phase: 06-missing-features
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/jest.config.js
  - backend/tests/reportController.test.ts
  - backend/tests/systemLog.test.ts
  - backend/tests/staffController.test.ts
  - backend/tests/payrollController.test.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
must_haves:
  truths:
    - Test framework is configured to run Jest tests
    - Test stubs are ready to be implemented
  artifacts:
    - path: backend/jest.config.js
      provides: Jest configuration
      min_lines: 5
  key_links:
    - from: backend/package.json
      to: backend/jest.config.js
      via: test script
---

<objective>
Set up Jest and test stubs for the missing features.
Purpose: Comply with the Nyquist validation strategy and prepare for test-driven validation.
Output: Jest configured and test files scaffolded.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-missing-features/06-PATTERNS.md
</execution_context>

<context>
@backend/package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Setup Jest</name>
  <files>backend/package.json, backend/jest.config.js</files>
  <read_first>
    - backend/package.json
  </read_first>
  <action>
    Install Jest and its TypeScript dependencies: `npm install --save-dev jest ts-jest @types/jest` in the backend.
    Create `backend/jest.config.js` to configure `ts-jest` for the Node environment.
    Update `backend/package.json` to include `"test": "jest"` in scripts.
  </action>
  <verify>
    <automated>cd backend && npx jest --version</automated>
  </verify>
  <done>
    - Jest is installed and configured
    - Running `npm test` triggers Jest
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Test Stubs</name>
  <files>backend/tests/reportController.test.ts, backend/tests/systemLog.test.ts, backend/tests/staffController.test.ts, backend/tests/payrollController.test.ts</files>
  <action>
    Create stub test files in `backend/tests/` for `reportController.test.ts`, `systemLog.test.ts`, `staffController.test.ts`, and `payrollController.test.ts` with basic failing or pending tests to be implemented in subsequent plans.
  </action>
  <verify>
    <automated>cd backend && npx jest --passWithNoTests</automated>
  </verify>
  <done>
    - Test stub files are created
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing | Test code is confined to development and testing environments. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-1 | Information Disclosure | Tests | mitigate | Ensure no sensitive data is hardcoded in test stubs. |
</threat_model>

<verification>
- `npm test` executes successfully, even if with 0 tests.
</verification>

<success_criteria>
- Jest is configured.
- Initial test stubs exist.
</success_criteria>

<output>
After completion, create `.planning/phases/06-missing-features/06-00-SUMMARY.md`
</output>