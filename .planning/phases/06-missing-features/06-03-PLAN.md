---
phase: 06-missing-features
plan: 03
type: execute
wave: 2
depends_on: [00, 02]
files_modified:
  - backend/package.json
  - backend/src/controllers/payrollController.ts
  - backend/src/routes/payrollRoutes.ts
autonomous: true
requirements:
  - FEAT-02
must_haves:
  truths:
    - Managers can export payroll data as an Excel file
    - Export file streams to the client without hanging
  artifacts:
    - path: backend/src/controllers/payrollController.ts
      provides: exportPayrollExcel controller
      min_lines: 150
    - path: backend/src/routes/payrollRoutes.ts
      provides: GET /export/:id endpoint
      min_lines: 30
  key_links:
    - from: backend/src/routes/payrollRoutes.ts
      to: backend/src/controllers/payrollController.ts
      via: Router GET mapping
---

<objective>
Implement a dedicated endpoint for managers to export payroll data in Excel format.
Purpose: Allow managers to securely download calculated payroll data for reporting and external compliance.
Output: A new authenticated GET endpoint that streams an .xlsx buffer.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-missing-features/06-PATTERNS.md
</execution_context>

<context>
@backend/src/controllers/payrollController.ts
@backend/src/routes/payrollRoutes.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install exceljs</name>
  <files>backend/package.json</files>
  <read_first>
    - backend/package.json
  </read_first>
  <action>
    Run the command to install the `exceljs` library in the backend.
    Execute: `cd backend && npm install exceljs && npm install --save-dev @types/exceljs`
  </action>
  <verify>
    <automated>cd backend && npm ls exceljs</automated>
  </verify>
  <done>
    - `backend/package.json` contains `"exceljs"` in its dependencies
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Payroll Export Endpoint</name>
  <files>backend/src/controllers/payrollController.ts, backend/src/routes/payrollRoutes.ts</files>
  <read_first>
    - backend/src/controllers/payrollController.ts
    - backend/src/routes/payrollRoutes.ts
  </read_first>
  <action>
    In `backend/src/controllers/payrollController.ts`:
    Reference the Error handling pattern and Imports pattern from 06-PATTERNS.md for the controller logic.
    1. Create an `exportPayrollExcel` async function.
    2. Retrieve the payroll period and related records (`staff`, `commissions`, `base pay`, `deductions`).
    3. Initialize a new `exceljs.Workbook()` and `addWorksheet`.
    4. Define columns: Staff Full Name, Payroll Period (Start Date - End Date), Base Pay, Commissions, Deductions, Net Pay, TIN, SSS.
    5. Set `res.setHeader('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')`.
    6. Set `res.setHeader('Content-Disposition', 'attachment; filename="Payroll_Report.xlsx"')`.
    7. Stream to response: `await workbook.xlsx.write(res); res.end();`
    
    In `backend/src/routes/payrollRoutes.ts`:
    Reference the Route Guard Pattern from 06-PATTERNS.md.
    1. Import `exportPayrollExcel`.
    2. Register `router.get('/export/:id', authorizeRoles('manager'), exportPayrollExcel);`
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/tests/payrollController.test.ts first</automated>
  </verify>
  <done>
    - `backend/src/routes/payrollRoutes.ts` contains `router.get('/export/:id'`
    - The export route is protected by `authorizeRoles('manager')`
    - `backend/src/controllers/payrollController.ts` contains `workbook.xlsx.write(res)`
    - `backend/src/controllers/payrollController.ts` sets the `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` content type
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Only authorized managers should download payroll exports. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-1 | Elevation of Privilege | payrollRoutes.ts | mitigate | Enforce `authorizeRoles('manager')` on the export endpoint. |
</threat_model>

<verification>
- Endpoint returns a valid .xlsx file when requested by a manager.
</verification>

<success_criteria>
- `exceljs` library is installed and used to generate the export.
- The route is securely restricted to managers.
</success_criteria>

<output>
After completion, create `.planning/phases/06-missing-features/06-03-SUMMARY.md`
</output>