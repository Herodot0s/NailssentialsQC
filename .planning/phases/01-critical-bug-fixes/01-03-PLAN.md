---
phase: 01-critical-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/pages/ManagerDashboard.tsx]
autonomous: true
requirements: [BUG-03]
must_haves:
  truths:
    - "ManagerDashboard setState uses proper union type instead of any"
    - "ActiveView type matches menuItems id values exactly"
  artifacts:
    - path: "frontend/src/pages/ManagerDashboard.tsx"
      provides: "ActiveView union type and proper type casting"
      contains: ["type ActiveView = ", "as ActiveView"]
  key_links:
    - from: "frontend/src/pages/ManagerDashboard.tsx"
      to: "menuItems"
      via: "ActiveView type"
      pattern: "type ActiveView = 'analytics' | 'staff' | 'attendance' | 'deductions' | 'payroll' | 'reviews'"
---

<objective>
Fix ManagerDashboard.tsx setState type mismatch where `as any` cast bypasses TypeScript type checking. Per D-05, define `ActiveView` union type inline and replace `as any` with `as ActiveView`.

Purpose: Restore TypeScript type safety for ManagerDashboard view state management.
Output: ManagerDashboard.tsx with proper ActiveView type definition and casting.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/PROJECT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/ROADMAP.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/STATE.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ActiveView union type</name>
  <read_first>frontend/src/pages/ManagerDashboard.tsx (lines 153-154, 410-417)</read_first>
  <acceptance_criteria>
    - `type ActiveView` is defined before the useState call
    - Union type includes exactly: 'analytics' | 'staff' | 'attendance' | 'deductions' | 'payroll' | 'reviews'
    - Union type matches menuItems array id values exactly
  </acceptance_criteria>
  <files>frontend/src/pages/ManagerDashboard.tsx</files>
  <action>Add ActiveView union type definition before the useState call (before line 153):
```typescript
type ActiveView = 'analytics' | 'staff' | 'attendance' | 'deductions' | 'payroll' | 'reviews';
```

Update the useState call (line 153) to use the new type:
```typescript
const [activeView, setActiveView] = useState<ActiveView>('analytics');
```</action>
  <verify>
    <automated>grep -c "type ActiveView = 'analytics' | 'staff' | 'attendance' | 'deductions' | 'payroll' | 'reviews'" frontend/src/pages/ManagerDashboard.tsx</automated>
    <automated>grep -c "useState<ActiveView>" frontend/src/pages/ManagerDashboard.tsx</automated>
  </verify>
  <done>ActiveView type defined and useState uses proper type parameter, both grep commands return 1.</done>
</task>

<task type="auto">
  <name>Task 2: Replace as any with as ActiveView</name>
  <read_first>frontend/src/pages/ManagerDashboard.tsx (line 444)</read_first>
  <acceptance_criteria>
    - No `as any` casts exist for setActiveView calls
    - onClick handlers use `as ActiveView` cast
    - menuItems id values are correctly typed as ActiveView
  </acceptance_criteria>
  <files>frontend/src/pages/ManagerDashboard.tsx</files>
  <action>Replace the onClick handler (line 444) from:
```typescript
onClick={() => setActiveView(item.id as any)}
```
To:
```typescript
onClick={() => setActiveView(item.id as ActiveView)}
```
Ensure all menuItems mapping uses this corrected cast.</action>
  <verify>
    <automated>grep -c "setActiveView(item.id as any)" frontend/src/pages/ManagerDashboard.tsx</automated>
    <automated>grep -c "setActiveView(item.id as ActiveView)" frontend/src/pages/ManagerDashboard.tsx</automated>
  </verify>
  <done>as any cast replaced with as ActiveView, grep for broken pattern returns 0, grep for correct pattern returns 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → React App | ManagerDashboard runs in browser, no sensitive data handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | ManagerDashboard.tsx | accept | No sensitive data handled, type safety improvement only |
</threat_model>

<verification>
1. Run TypeScript compilation: `cd frontend && npx tsc --noEmit`
2. Verify no TypeScript errors related to ActiveView type
3. Confirm ManagerDashboard renders correctly with view switching
</verification>

<success_criteria>
- No `as any` casts in ManagerDashboard.tsx for ActiveView
- TypeScript compilation passes without ActiveView-related errors
- ManagerDashboard view switching works correctly with proper type safety
</success_criteria>

<output>
After completion, create `C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-03-SUMMARY.md`
</output>
