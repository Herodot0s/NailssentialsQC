---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/Navbar.tsx]
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Navbar dropdown menu renders without JSX syntax errors"
    - "All DropdownMenuTrigger and DropdownMenuItem components have valid render prop syntax"
  artifacts:
    - path: "frontend/src/components/Navbar.tsx"
      provides: "Fixed JSX syntax for Base UI DropdownMenu components"
      contains: ["DropdownMenuTrigger render={() =>", "DropdownMenuItem render={() =>"]
  key_links:
    - from: "frontend/src/components/Navbar.tsx"
      to: "@/components/ui/dropdown-menu"
      via: "render prop"
      pattern: "DropdownMenuTrigger render=\\{|DropdownMenuItem render=\\{"
---

<objective>
Fix JSX syntax errors in Navbar.tsx where missing spaces between component names and `render` prop cause React rendering failures. Per D-03, fix in place without migrating to Radix DropdownMenu.

Purpose: Eliminate JSX syntax errors that prevent Navbar dropdown menu from rendering correctly.
Output: Navbar.tsx with correct DropdownMenu component syntax.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/PROJECT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/ROADMAP.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/STATE.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-PATTERNS.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DropdownMenuTrigger render prop syntax</name>
  <read_first>frontend/src/components/Navbar.tsx (lines 105-117)</read_first>
  <acceptance_criteria>
    - No occurrences of `DropdownMenuTrigger render={>` exist in Navbar.tsx
    - Line 105+ contains `<DropdownMenuTrigger` followed by newline and `render={() =>`
    - Existing Button and Avatar content inside trigger is preserved exactly
  </acceptance_criteria>
  <files>frontend/src/components/Navbar.tsx</files>
  <action>Add space between `DropdownMenuTrigger` and `render` prop, wrap render value in arrow function returning JSX. Replace the broken line 105 `<DropdownMenuTrigger render={>` with:
```tsx
<DropdownMenuTrigger
  render={() => (
    <Button variant="ghost" className="relative h-10 w-10 rounded-full hover:bg-primary/5 transition-colors">
      <Avatar className="h-10 w-10">
        <AvatarFallback className="bg-primary-ultra text-primary font-serif font-bold text-sm border-[0.5px] border-primary/20">
          {user?.fullName ? (
            getInitials(user.fullName)
          ) : (
            <User className="h-4 w-4 stroke-[1.5]" />
          )}
        </AvatarFallback>
      </Avatar>
    </Button>
  )}
</DropdownMenuTrigger>
```
Preserve all existing inner content exactly as is.</action>
  <verify>
    <automated>grep -c "DropdownMenuTrigger render=" frontend/src/components/Navbar.tsx</automated>
  </verify>
  <done>DropdownMenuTrigger render prop is correctly formatted with space and arrow function, returns 0 when grepping for broken pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DropdownMenuItem render prop syntax</name>
  <read_first>frontend/src/components/Navbar.tsx (lines 130, 136, 143, 150, 206, 210-211)</read_first>
  <acceptance_criteria>
    - No occurrences of `DropdownMenuItemrender=` exist in Navbar.tsx
    - All 7 DropdownMenuItem components have space between component name and `render` prop
    - Each DropdownMenuItem uses self-closing tag `/>` with arrow function render prop
    - Existing Link, icon, and span content inside each item is preserved exactly
  </acceptance_criteria>
  <files>frontend/src/components/Navbar.tsx</files>
  <action>For all 7 broken DropdownMenuItem occurrences (lines 130, 136, 143, 150, 206, 210, 211), replace `<DropdownMenuItemrender={` with:
```tsx
<DropdownMenuItem
  render={() => (
    <Link to="[original-path]">
      {/* original icon and span content */}
    </Link>
  )}
/>
```
Preserve the original `to` path, icon component, and span text exactly. Ensure self-closing `/>` is used. Example for line 130:
```tsx
<DropdownMenuItem
  render={() => (
    <Link to="/profile">
      <User className="mr-3 h-4 w-4 stroke-[1.5]" />
      <span className="text-xs font-medium">Profile Settings</span>
    </Link>
  )}
/>
```</action>
  <verify>
    <automated>grep -c "DropdownMenuItemrender=" frontend/src/components/Navbar.tsx</automated>
  </verify>
  <done>All DropdownMenuItem components have correct JSX syntax, returns 0 when grepping for broken pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → React App | Navbar renders in browser, no sensitive data handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | Navbar.tsx | accept | No sensitive data handled, UI-only syntax fix |
| T-01-01-02 | Tampering | Navbar.tsx | accept | No data modification, only syntax correction |
</threat_model>

<verification>
1. Open browser to application URL
2. Click user avatar to open Navbar dropdown menu
3. Verify dropdown renders correctly with all menu items
4. Check browser console for React JSX syntax errors (should be none)
</verification>

<success_criteria>
- Navbar.tsx has no JSX syntax errors (grep returns 0 for broken patterns)
- DropdownMenu renders correctly in browser without console errors
- All existing DropdownMenu functionality (links, avatars) is preserved exactly
</success_criteria>

<output>
After completion, create `C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md`
</output>
