---
phase: 08-payroll-models-backend-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/prisma/schema.prisma"]
autonomous: true
requirements: ["PAY-01", "PAY-04"]

must_haves:
  truths:
    - "Schema contains new commission fields for StaffProfile"
    - "Schema restricts DeductionLog types or uses enum"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Updated database schema"
      contains: "base_commission_rate"
  key_links: []
---

<objective>
Update database schema to support specific commission rates and categorized deductions, then push to the database.

Purpose: Backend foundation for the new payroll system.
Output: Updated schema.prisma and synced database schema.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-payroll-models-backend-foundations/08-CONTEXT.md
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files. The planner MUST include
a `[BLOCKING]` task that runs the database schema push command AFTER all schema file
modifications are complete but BEFORE verification.

- ORM detected: Prisma
- Push command: npx prisma db push
- Non-TTY workaround: npx prisma db push --accept-data-loss
- If push requires interactive prompts that cannot be suppressed, flag the task for
  manual intervention with `autonomous: false`

This task is mandatory — the phase CANNOT pass verification without it. Build and
type checks will pass without the push (types come from config, not the live database),
creating a false-positive verification state.
</schema_push_requirement>

<tasks>

<task type="auto">
  <name>Task 1: Update schema.prisma</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>
    1. In `StaffProfile` model, add `base_commission_rate Decimal @default(0.10) @db.Decimal(5, 2)` and `commission_tier Int?`.
    2. Add enum `DeductionType { cash_advance \n loan \n uniform \n reloan \n lates_early_out \n other }` (formatted properly for Prisma).
    3. In `DeductionLog` model, change `type String` to `type DeductionType`.
  </action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - `StaffProfile` has `base_commission_rate` and `commission_tier` fields.
    - `DeductionType` enum is created.
    - `DeductionLog.type` uses `DeductionType`.
  </acceptance_criteria>
  <done>Prisma schema validation passes</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Schema Push Required</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>Run the database push command: `cd backend && npx prisma db push --accept-data-loss` to sync the new models with the live database.</action>
  <verify>Check the output for success message</verify>
  <acceptance_criteria>
    - `npx prisma db push` executes successfully
    - Database is fully synced with the new schema
  </acceptance_criteria>
  <done>Database schema updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma db push` was successfully run
</verification>

<success_criteria>
- Schema correctly represents new commission fields and deduction types
- Database is updated without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-payroll-models-backend-foundations/08-01-SUMMARY.md`
</output>
