---
phase: 08-payroll-models-backend-foundations
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["backend/src/controllers/staffController.ts", "backend/src/routes/staffRoutes.ts", "backend/src/controllers/payrollController.ts", "backend/src/routes/payrollRoutes.ts"]
autonomous: true
requirements: ["PAY-01", "PAY-02", "PAY-04"]

must_haves:
  truths:
    - "Manager can update StaffProfile base_commission_rate and commission_tier"
    - "Manager can manage DeductionLogs"
    - "Manager can generate a 7-day PayrollPeriod"
  artifacts:
    - path: "backend/src/controllers/staffController.ts"
      provides: "Staff update endpoint"
    - path: "backend/src/controllers/payrollController.ts"
      provides: "Deduction and Period logic"
  key_links: []
---

<objective>
Implement API endpoints for updating staff commissions, managing deduction logs, and manually generating weekly payroll periods.

Purpose: Allow managers to configure and trigger payroll processes securely.
Output: Updated Express controllers and routes.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-payroll-models-backend-foundations/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Staff API for Commissions</name>
  <files>backend/src/controllers/staffController.ts, backend/src/routes/staffRoutes.ts</files>
  <read_first>backend/src/controllers/staffController.ts</read_first>
  <action>
    1. Update the staff update controller (e.g., `updateStaff`) to accept and update `base_commission_rate` and `commission_tier`.
    2. Ensure only managers can update these fields (verify `req.user.role === 'manager'`).
  </action>
  <verify>tsc --noEmit</verify>
  <acceptance_criteria>
    - StaffController handles new commission fields
    - Manager role check is enforced
  </acceptance_criteria>
  <done>Compilation passes with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deduction Log CRUD</name>
  <files>backend/src/controllers/payrollController.ts, backend/src/routes/payrollRoutes.ts</files>
  <read_first>backend/src/controllers/payrollController.ts</read_first>
  <action>
    1. Create/update endpoints in `payrollController.ts` for managing deductions (`POST /deductions`, `GET /deductions`, `DELETE /deductions/:id`).
    2. Enforce the new `DeductionType` enum constraints when creating deductions.
    3. Add these routes to `payrollRoutes.ts` with manager authentication.
  </action>
  <verify>tsc --noEmit</verify>
  <acceptance_criteria>
    - Endpoints created for creating, viewing, and deleting deductions
    - Roles are validated
  </acceptance_criteria>
  <done>Compilation passes</done>
</task>

<task type="auto">
  <name>Task 3: Implement Payroll Period Generation</name>
  <files>backend/src/controllers/payrollController.ts, backend/src/routes/payrollRoutes.ts</files>
  <read_first>backend/src/controllers/payrollController.ts</read_first>
  <action>
    1. Create `generateNextPeriod` endpoint (`POST /periods/generate`).
    2. Logic: Query latest `PayrollPeriod`. If found, `start_date` = latest `end_date` + 1 day. `end_date` = new `start_date` + 6 days. If none found, use a provided `start_date` from request body and `end_date` = `start_date` + 6 days.
    3. Save the new period and return it.
    4. Bind endpoint to `payrollRoutes.ts` protected by manager auth.
  </action>
  <verify>tsc --noEmit</verify>
  <acceptance_criteria>
    - Endpoint creates 7-day period accurately
    - Endpoint is protected
  </acceptance_criteria>
  <done>Compilation passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run build` succeeds without TS errors
- [ ] All new routes are mounted correctly
</verification>

<success_criteria>
- APIs support new features required by PAY-01, PAY-02, and PAY-04
- Type safety maintained across controllers
</success_criteria>

<output>
After completion, create `.planning/phases/08-payroll-models-backend-foundations/08-02-SUMMARY.md`
</output>
