---
phase: 4
plan: "04-02"
title: "Manager Dashboard Responsive Sidebar & Overview"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/components/dashboard/ManagerSidebar.tsx
  - frontend/src/components/dashboard/OverviewView.tsx
  - frontend/src/components/dashboard/OverviewCards.tsx
  - frontend/src/components/dashboard/AppointmentTimeline.tsx
  - frontend/src/pages/ManagerDashboard.tsx
autonomous: true
requirements:
  - UI-03
---

<objective>
Add responsive sidebar collapse behavior (icon-only at tablet, hidden with hamburger on mobile) and create the "Today's Overview" landing view with stat cards and appointment timeline per D-04 and D-08.
</objective>

<tasks>

<task id="T1" type="execute">
<title>Add responsive collapse to ManagerSidebar</title>
<read_first>
- frontend/src/components/dashboard/ManagerSidebar.tsx
- .planning/phases/04-operations-interface-overhaul/04-UI-SPEC.md (Responsive Breakpoints section)
</read_first>
<files>
- frontend/src/components/dashboard/ManagerSidebar.tsx
- frontend/src/components/dashboard/types.ts
</files>
<action>
Update `ManagerSidebar` to support 3 responsive states:

1. **Desktop (> 1024px):** Full sidebar `w-64` with icon + label (current behavior).
2. **Tablet (768-1024px):** Collapsed sidebar `w-12` showing icons only. Nav items show only the icon, no text label. Brand block shows only "N" initial. Footer shows only the green dot.
3. **Mobile (< 768px):** Sidebar is hidden. A hamburger button (Menu icon from lucide-react) appears in the content header area. Clicking it opens the sidebar as a Sheet overlay from the left.

Update `ManagerSidebarProps` in types.ts to add:
```typescript
export interface ManagerSidebarProps {
  activeView: ActiveView;
  onViewChange: (view: ActiveView) => void;
  collapsed?: boolean;        // tablet collapsed state
  mobileOpen?: boolean;       // mobile sheet open state
  onMobileToggle?: () => void; // toggle mobile sheet
}
```

Implementation approach:
- Use `window.matchMedia` or Tailwind responsive classes (`hidden lg:flex`, `lg:w-64 md:w-12`) to handle breakpoints
- For mobile: wrap sidebar content in a `Sheet` from `@/components/ui/sheet` with `side="left"` when `mobileOpen` is true
- Desktop and tablet render the sidebar inline, mobile renders it as a Sheet overlay
- Add a `sidebarCollapsed` state to ManagerDashboard.tsx (parent) that tracks whether the sidebar is in collapsed mode based on viewport width
</action>
<acceptance_criteria>
- At viewport > 1024px: sidebar shows full width (w-64) with icon + label text
- At viewport 768-1024px: sidebar collapses to icon-only (w-12)
- At viewport < 768px: sidebar is hidden, hamburger icon visible
- Mobile hamburger opens sidebar as Sheet overlay from left
- `ManagerSidebarProps` in types.ts contains `collapsed?: boolean`
- `ManagerSidebarProps` in types.ts contains `mobileOpen?: boolean`
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Create OverviewCards component for Today's Overview</title>
<read_first>
- frontend/src/components/dashboard/OverviewView.tsx
- .planning/phases/04-operations-interface-overhaul/04-UI-SPEC.md (Layout Contract - Overview landing)
- .planning/phases/04-operations-interface-overhaul/04-CONTEXT.md (D-04)
</read_first>
<files>
- frontend/src/components/dashboard/OverviewCards.tsx
</files>
<action>
Create `frontend/src/components/dashboard/OverviewCards.tsx` — the "Today's Overview" stat cards grid per D-04.

The component shows 4 glanceable cards in a responsive grid:
- **Today's Revenue:** `₱{totalRevenue}` with trend indicator
- **Active Rituals:** `{transactionCount}` with online/walk-in breakdown
- **Active Staff:** Count of checked-in staff (from staffMembers where attendance status is active)
- **Pending Reviews:** Count of unmoderated reviews

Card styling per UI-SPEC:
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8`
- Each card: `rounded-none border-none shadow-sm bg-white overflow-hidden group` with `relative` positioning
- Left accent stripe: `<div className="absolute top-0 left-0 w-1 h-full bg-primary" />`
- Stat numbers: `text-4xl font-serif font-light`
- Labels: `text-[9px] font-bold uppercase tracking-widest text-muted-foreground`
- Card padding: `p-8` for spacious overview feel (D-08)

Props:
```typescript
interface OverviewCardsProps {
  salesStats: SalesStats | null;
  activeStaffCount: number;
  pendingReviewCount: number;
}
```
</action>
<acceptance_criteria>
- `frontend/src/components/dashboard/OverviewCards.tsx` exists
- File exports `OverviewCards` component
- Component renders 4 stat cards in a responsive grid
- Cards have left accent stripe (`w-1 h-full bg-primary`)
- Stat numbers use `text-4xl font-serif font-light`
- Labels use `text-[9px] font-bold uppercase tracking-widest`
- Grid uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Create AppointmentTimeline component</title>
<read_first>
- frontend/src/pages/StaffDashboard.tsx (lines 262-304 for existing appointment table pattern)
- frontend/src/api/apiClient.ts (getAppointments function)
- .planning/phases/04-operations-interface-overhaul/04-CONTEXT.md (D-04)
</read_first>
<files>
- frontend/src/components/dashboard/AppointmentTimeline.tsx
</files>
<action>
Create `frontend/src/components/dashboard/AppointmentTimeline.tsx` — a compact today's appointment timeline for the Manager Overview landing view (D-04).

This shows today's appointments as a vertical timeline below the stat cards. Each row shows time slot, client name, assigned staff, service, and status badge.

```typescript
interface AppointmentTimelineProps {
  appointments: Array<{
    id: number;
    customer: { full_name: string };
    status: string;
    appointment_date: string;
    items: Array<{
      id: number;
      service: { name: string };
      staff_id: number;
      start_time: string;
      end_time: string;
      status: string;
    }>;
  }>;
  staffMembers: StaffMember[];
}
```

Layout:
- Card with `rounded-none border-none shadow-sm bg-white`
- Header: `font-serif text-2xl font-light` "Today's Schedule"
- Description: `text-[10px] uppercase font-bold tracking-widest` with appointment count
- Table with columns: Timeline | Client | Treatment | Staff | Status
- Table header: `bg-muted/30`, `text-[9px] uppercase tracking-widest font-bold`
- Row hover: `hover:bg-primary-ultra/10 border-primary/5 transition-colors`
- Empty state: "No rituals scheduled for this cycle." centered italic text
- Current appointment (closest to now) gets a subtle left border highlight: `border-l-2 border-primary`

Filter appointments to today only. Map staff_id to staff name from staffMembers prop.
</action>
<acceptance_criteria>
- `frontend/src/components/dashboard/AppointmentTimeline.tsx` exists
- File exports `AppointmentTimeline` component
- Component filters to today's appointments
- Table has 5 columns: Timeline, Client, Treatment, Staff, Status
- Empty state shows "No rituals scheduled for this cycle."
- Current/next appointment has visual highlight
</acceptance_criteria>
</task>

<task id="T4" type="execute" depends_on="T1,T2,T3">
<title>Integrate responsive sidebar and new overview into ManagerDashboard</title>
<read_first>
- frontend/src/pages/ManagerDashboard.tsx
- frontend/src/components/dashboard/ManagerSidebar.tsx
- frontend/src/components/dashboard/OverviewCards.tsx
- frontend/src/components/dashboard/AppointmentTimeline.tsx
- frontend/src/components/dashboard/OverviewView.tsx
</read_first>
<files>
- frontend/src/pages/ManagerDashboard.tsx
- frontend/src/components/dashboard/OverviewView.tsx
</files>
<action>
1. **ManagerDashboard.tsx:** Add state for sidebar responsive behavior:
   ```tsx
   const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
   const [mobileSidebarOpen, setMobileSidebarOpen] = useState(false);
   ```
   Pass these to `<ManagerSidebar collapsed={sidebarCollapsed} mobileOpen={mobileSidebarOpen} onMobileToggle={() => setMobileSidebarOpen(!mobileSidebarOpen)} />`.

   Add a hamburger button in the main header that is only visible on mobile (`md:hidden`):
   ```tsx
   <button onClick={() => setMobileSidebarOpen(true)} className="md:hidden">
     <Menu className="h-6 w-6" />
   </button>
   ```

   Adjust main content padding: `p-4 md:p-6 lg:p-10` for responsive spacing.

2. **OverviewView.tsx:** Update to use the new `OverviewCards` and `AppointmentTimeline` components at the top of the analytics view, before the existing charts section. The overview now shows:
   - `<OverviewCards>` (stat cards grid)
   - `<AppointmentTimeline>` (today's schedule)
   - Existing: DrillDownLineChart + PieChart (Revenue Split)

   Add new props to `OverviewViewProps`: `activeStaffCount: number`, `pendingReviewCount: number`, `appointments: Appointment[]`, `staffMembers: StaffMember[]`.

3. **ManagerDashboard.tsx:** Fetch today's appointments in fetchData (use existing `getAppointments` from apiClient if not already called, or reuse existing appointment data). Pass `activeStaffCount` (count of staff with active attendance today) and `pendingReviewCount` (count of reviews where `approved === null`) to OverviewView.
</action>
<acceptance_criteria>
- ManagerDashboard.tsx contains `sidebarCollapsed` and `mobileSidebarOpen` state
- ManagerDashboard.tsx has a hamburger button with `md:hidden` class
- OverviewView renders `OverviewCards` component
- OverviewView renders `AppointmentTimeline` component
- Main content area uses responsive padding `p-4 md:p-6 lg:p-10`
- `npx tsc --noEmit` passes
- `npm run build` completes successfully
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes (zero type errors)
- [ ] `npm run build` completes successfully
- [ ] Sidebar collapses to icon-only at tablet breakpoint (768-1024px)
- [ ] Sidebar is hidden on mobile (< 768px) with hamburger toggle
- [ ] Overview landing shows 4 stat cards in responsive grid
- [ ] Appointment timeline shows today's schedule below stat cards
- [ ] Existing charts (DrillDown, PieChart) still render correctly
</verification>

<must_haves>
<truths>
- D-01: Manager Dashboard uses persistent sidebar navigation + content area layout
- D-04: Default landing view is "Today's Overview" with stat cards and appointment timeline
- D-08: Hybrid density — overview pages are spacious with large cards
- D-09: Details accessed through drill-down, not crammed into overview
</truths>
<goals>
- UI-03: Redesign Manager & Staff Dashboards (responsive sidebar and overview)
</goals>
</must_haves>
