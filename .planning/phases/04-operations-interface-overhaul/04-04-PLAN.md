---
phase: 4
plan: "04-04"
title: "Visual Polish & Integration Testing"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - frontend/src/pages/ManagerDashboard.tsx
  - frontend/src/pages/StaffDashboard.tsx
  - frontend/src/components/dashboard/ManagerSidebar.tsx
  - frontend/src/components/dashboard/OverviewView.tsx
  - frontend/src/components/dashboard/MobileCheckIn.tsx
autonomous: true
requirements:
  - UI-03
---

<objective>
Apply final visual polish per UI-SPEC animation contract, verify all view transitions, ensure consistent density approach (D-08), and validate the complete integrated experience across both dashboards. This plan addresses cross-cutting visual concerns that span all components created in waves 1-2.
</objective>

<tasks>

<task id="T1" type="execute">
<title>Apply animation contract to view transitions</title>
<read_first>
- .planning/phases/04-operations-interface-overhaul/04-UI-SPEC.md (Animation Contract section)
- frontend/src/components/dashboard/OverviewView.tsx
- frontend/src/components/dashboard/ManagerSidebar.tsx
- frontend/src/pages/ManagerDashboard.tsx
</read_first>
<files>
- frontend/src/pages/ManagerDashboard.tsx
- frontend/src/components/dashboard/OverviewView.tsx
- frontend/src/components/dashboard/OverviewCards.tsx
- frontend/src/components/dashboard/AppointmentTimeline.tsx
- frontend/src/components/dashboard/MobileCheckIn.tsx
</files>
<action>
Apply the UI-SPEC animation contract across all new components:

1. **View switch animation (sidebar):** When `activeView` changes, the content area should use `animate-in fade-in duration-700`. Wrap each view's content in a div with this class. The key pattern is already established in existing views (e.g., line 477 of original ManagerDashboard.tsx: `<div className="space-y-12 animate-in fade-in duration-700">`). Ensure ALL views have this wrapper, including the newly integrated OverviewView.

2. **Page content load:** The header in ManagerDashboard.tsx already uses `animate-in fade-in slide-in-from-top-4 duration-700` (line 451). Ensure this is preserved.

3. **Sidebar item hover:** Already has `transition-all` on buttons. Verify `transition-colors duration-150` is present on all interactive sidebar elements.

4. **Table row hover:** Verify all tables use `hover:bg-primary-ultra/10 border-primary/5 transition-colors` per UI-SPEC.

5. **Check-in pulse:** The clock container in MobileCheckIn should use `animate-pulse` when `isCheckedIn` is true (existing pattern from StaffDashboard.tsx line 231).

6. **Loading spinner:** Verify both dashboards use `animate-spin` on `Loader2` icon during loading states.

7. **System status dot:** Verify the green dot in ManagerSidebar footer uses `animate-pulse`.

No new animations should be added beyond the UI-SPEC contract. The goal is consistency verification.
</action>
<acceptance_criteria>
- Every view wrapped in `animate-in fade-in duration-700` when active
- Header uses `animate-in fade-in slide-in-from-top-4 duration-700`
- Sidebar buttons have `transition-all` class
- Table rows have `transition-colors` class
- Clock container uses `animate-pulse` when checked in
- Loading spinner uses `animate-spin`
- System status dot uses `animate-pulse`
</acceptance_criteria>
</task>

<task id="T2" type="execute">
<title>Verify density approach across all views</title>
<read_first>
- .planning/phases/04-operations-interface-overhaul/04-UI-SPEC.md (Layout Contract - Density approach D-08)
- .planning/phases/04-operations-interface-overhaul/04-CONTEXT.md (D-08, D-09)
- frontend/src/components/dashboard/OverviewView.tsx
- frontend/src/components/dashboard/StaffTable.tsx
- frontend/src/components/dashboard/PayrollTable.tsx
- frontend/src/components/dashboard/AttendanceLedger.tsx
</read_first>
<files>
- frontend/src/components/dashboard/OverviewView.tsx
- frontend/src/components/dashboard/OverviewCards.tsx
</files>
<action>
Verify and adjust density approach per D-08:

**Spacious pages (overview/summary):**
- OverviewCards: Ensure stat cards have `p-8` internal padding (32px)
- OverviewCards: Stat numbers use `text-4xl font-serif font-light` (large, serif)
- OverviewCards: Gap between cards is `gap-8` (32px)
- Overview section spacing: `space-y-12` between major sections (48px)
- Heading in overview: `font-serif text-2xl font-light` for section headers

**Compact pages (drill-down/data-dense):**
Verify existing sub-components follow compact density (no changes needed, just verification):
- StaffTable: Tight row padding, compact badges
- PayrollTable: Dense financial data, compact numbers
- AttendanceLedger: Operational table with compact rows
- ReviewModeration: Compact review cards

**Cross-cutting:**
- Section spacing within overview: `gap-8` for card grids, `gap-12` for between chart sections
- Ensure no spacious styling leaked into compact views and vice versa
</action>
<acceptance_criteria>
- OverviewCards stat cards have `p-8` padding
- OverviewCards stat numbers use `text-4xl font-serif`
- Overview uses `space-y-12` between major sections
- Overview uses `gap-8` between stat cards
- Charts section uses `gap-12` between chart groups
</acceptance_criteria>
</task>

<task id="T3" type="execute">
<title>Full integration verification</title>
<read_first>
- frontend/src/pages/ManagerDashboard.tsx
- frontend/src/pages/StaffDashboard.tsx
- frontend/src/components/dashboard/types.ts
</read_first>
<files>
- frontend/src/pages/ManagerDashboard.tsx
- frontend/src/pages/StaffDashboard.tsx
</files>
<action>
Run comprehensive verification:

1. **TypeScript compilation:** Run `npx tsc --noEmit` and fix any type errors.

2. **Build verification:** Run `npm run build` and fix any build errors.

3. **Import graph verification:** Ensure all new components are correctly imported:
   - ManagerDashboard imports: ManagerSidebar, OverviewView, DeductionsView, StaffDetailSheet, AddStaffDialog, ShiftEditDialog, PayrollRunDialog, DeductionEntryDialog, StaffTable, PayrollTable, AttendanceLedger, ReviewModeration, ManageExhibits, SalarySlipModal
   - StaffDashboard imports: MobileCheckIn
   - OverviewView imports: OverviewCards, AppointmentTimeline, DrillDownLineChart

4. **Prop threading verification:** Ensure all required props are passed through:
   - ManagerDashboard → ManagerSidebar: `activeView`, `onViewChange`, `collapsed`, `mobileOpen`, `onMobileToggle`
   - ManagerDashboard → OverviewView: `salesStats`, `historicalData`, `selectedCategory`, `onCategorySelect`, `onCategoryReset`, `activeStaffCount`, `pendingReviewCount`, `appointments`, `staffMembers`
   - StaffDashboard → MobileCheckIn: `isCheckedIn`, `checkInTime`, `currentTime`, `onCheckIn`, `onCheckOut`

5. **Fix any remaining issues** found during compilation or build.
</action>
<acceptance_criteria>
- `npx tsc --noEmit` exits with code 0
- `npm run build` exits with code 0
- All new components have valid imports with no circular dependencies
- No unused imports remain in ManagerDashboard.tsx
- No unused imports remain in StaffDashboard.tsx
- ManagerDashboard.tsx is under 400 lines
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes (zero type errors)
- [ ] `npm run build` completes successfully
- [ ] All 7 manager sidebar views render and transition smoothly
- [ ] Manager overview shows stat cards + appointment timeline + charts
- [ ] Staff mobile check-in overlay works correctly
- [ ] Density approach is consistent: spacious overview, compact drill-downs
- [ ] All animations match UI-SPEC contract (no extra, no missing)
- [ ] ManagerDashboard.tsx final line count < 400
</verification>

<must_haves>
<truths>
- D-01: Manager Dashboard uses persistent sidebar navigation + content area layout
- D-02: Monolithic ManagerDashboard.tsx decomposed into separate view components
- D-03: Staff Dashboard keeps existing tab structure
- D-04: Default landing is "Today's Overview" with stat cards and appointment timeline
- D-06: Check-in uses full-screen takeover on mobile
- D-07: Appointments display with current appointment highlighted
- D-08: Hybrid density — spacious overview, compact drill-downs
- D-09: Details accessed through drill-down navigation, not crammed into overview
</truths>
<goals>
- UI-03: Redesign Manager & Staff Dashboards
</goals>
</must_haves>
