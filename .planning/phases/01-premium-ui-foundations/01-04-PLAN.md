---
phase: "01"
plan: "04"
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: ["frontend/src/components/ui/button.tsx", "frontend/src/components/ui/card.tsx", "frontend/src/App.tsx"]
autonomous: false
requirements: ["UI-01", "UI-04"]

must_haves:
  truths:
    - "Buttons and cards use new radii (rounded-xl, rounded-3xl)"
    - "Page transitions are active across all routes"
    - "Buttons exhibit subtle scale animations on hover/tap"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Global animation orchestration with AnimatePresence"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Premium button with motion states"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/motion/PageTransition.tsx"
      via: "Route wrapping"
---

<objective>
Apply the new tokens and animations to core UI elements and orchestrate page transitions globally.

Purpose: Finalize the foundation by wiring tokens and animations into the live app.
Output: Refactored components and animated routing.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-premium-ui-foundations/01-CONTEXT.md
@.planning/phases/01-premium-ui-foundations/01-UI-SPEC.md
@.planning/phases/01-premium-ui-foundations/01-PATTERNS.md
@frontend/src/index.css (updated in plan 01-02)
@frontend/src/components/motion/PageTransition.tsx (created in plan 01-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor UI Components (Button & Card)</name>
  <files>frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx
    - frontend/src/components/ui/card.tsx
    - .planning/phases/01-premium-ui-foundations/01-CONTEXT.md
  </read_first>
  <action>
    Refactor existing shadcn components to match the Premium UI spec.
    
    1. Update frontend/src/components/ui/button.tsx:
       - Change default radius in buttonVariants from 'rounded-none' to 'rounded-xl'.
       - Import { motion } from 'framer-motion'.
       - Convert Button component to use motion.button.
       - Add motion props:
         whileHover={{ scale: 1.02 }}
         whileTap={{ scale: 0.98 }}
         transition={{ duration: 0.2, ease: [0.32, 0.72, 0, 1] }}
    
    2. Update frontend/src/components/ui/card.tsx:
       - Update Card className to use 'rounded-3xl' (instead of lg) and 'shadow-premium'.
       - Remove any hardcoded 'rounded-none' or legacy shadow classes.
  </action>
  <verify>
    <automated>grep "rounded-xl" frontend/src/components/ui/button.tsx</automated>
  </verify>
  <done>Base UI components updated with premium tokens and motion</done>
  <acceptance_criteria>
    - button.tsx uses motion.button and contains whileHover
    - button.tsx default radius is rounded-xl
    - card.tsx uses rounded-3xl and shadow-premium
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Orchestrate Global Page Transitions</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/components/motion/PageTransition.tsx
    - .planning/phases/01-premium-ui-foundations/01-RESEARCH.md
  </read_first>
  <action>
    Integrate PageTransition into the main application shell.
    
    1. In frontend/src/App.tsx:
       - Import { AnimatePresence } from 'framer-motion'.
       - Import { PageTransition } from '@/components/motion/PageTransition'.
       - Import { useLocation } from 'react-router-dom'.
    2. Wrap the <Routes> component with <AnimatePresence mode="wait">.
    3. Pass location={location} key={location.pathname} to <Routes>.
    4. Wrap each Route's element with <PageTransition>. (Note: If there are many routes, consider a higher-level wrapper inside a Layout component if it exists, otherwise wrap individual primary routes: Home, Login, Register, Dashboards).
  </action>
  <verify>
    <automated>grep "AnimatePresence" frontend/src/App.tsx</automated>
  </verify>
  <done>Routes orchestrated with framer-motion transitions</done>
  <acceptance_criteria>
    - App.tsx contains AnimatePresence
    - Routes uses location.pathname as key
    - Primary routes are wrapped in PageTransition
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Premium UI Foundations including design tokens, button/card redesigns, and page transitions.</ what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev:frontend
    2. Visit the landing page and verify the new Rausch color (#FF385C) on buttons.
    3. Verify buttons have rounded corners (12px) and scale slightly when hovered/clicked.
    4. Verify cards have large rounded corners (32px).
    5. Navigate between Login and Register pages and verify the smooth "Subtle Slide" transition (vertical lift + fade).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| React Component Tree | AnimatePresence manages the lifecycle of components during route changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | App.tsx | mitigate | Use 'mode="wait"' in AnimatePresence to ensure only one page transition happens at a time, preventing DOM bloat. |
</threat_model>

<verification>
Manual visual check of transitions and micro-interactions.
</verification>

<success_criteria>
The application feels premium with consistent tokens and smooth animations applied across major routes and components.
</success_criteria>

<output>
After completion, create .planning/phases/01-premium-ui-foundations/01-04-SUMMARY.md
</output>
