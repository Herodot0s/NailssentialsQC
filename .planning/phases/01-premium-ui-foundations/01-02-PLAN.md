---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["frontend/src/index.css", "frontend/tailwind.config.js"]
autonomous: true
requirements: ["UI-01"]

must_haves:
  truths:
    - "Global theme uses Rausch (#FF385C) and Muted Rausch (#FFF1F2)"
    - "Containers and cards have organic radii (24px-32px)"
    - "Typography uses Inter and Playfair Display"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "CSS variable definitions for Rausch and radii"
    - path: "frontend/tailwind.config.js"
      provides: "Theme mapping for new tokens and font families"
  key_links:
    - from: "frontend/src/index.css"
      to: "frontend/tailwind.config.js"
      via: "CSS variables mapped to theme"
---

<objective>
Implement the core design tokens in CSS and Tailwind configuration to enable the Rausch palette and organic radii.

Purpose: Establish the visual foundation for the Premium overhaul.
Output: Updated global CSS and Tailwind theme.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-premium-ui-foundations/01-CONTEXT.md
@.planning/phases/01-premium-ui-foundations/01-UI-SPEC.md
@.planning/phases/01-premium-ui-foundations/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Global CSS Variables</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css
    - .planning/phases/01-premium-ui-foundations/01-UI-SPEC.md
  </read_first>
  <action>
    Update frontend/src/index.css to include the premium design tokens as CSS variables in the :root section.
    
    Add or replace variables:
    :root {
      /* Brand Rausch Palette */
      --brand-rausch: #FF385C;
      --brand-rausch-muted: #FFF1F2;
      --bg-canvas: #FFFFFF;
      
      /* Radius System */
      --radius-container: 32px;
      --radius-utility: 12px;
      --radius-data: 8px;
      
      /* Shadows */
      --shadow-premium: 0 8px 28px rgba(0, 0, 0, 0.08);
    }
    
    Add a helper class for the featured border:
    .featured-border {
      border: 1.5px solid var(--brand-rausch);
      box-shadow: 0 0 0 4px var(--brand-rausch-muted);
    }
  </action>
  <verify>
    <automated>grep -i "#FF385C" frontend/src/index.css</automated>
  </verify>
  <done>CSS variables defined in index.css</done>
  <acceptance_criteria>
    - frontend/src/index.css contains --brand-rausch: #FF385C
    - frontend/src/index.css contains --radius-container: 32px
    - frontend/src/index.css contains .featured-border class
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Map Tokens to Tailwind Theme</name>
  <files>frontend/tailwind.config.js</files>
  <read_first>
    - frontend/tailwind.config.js
    - .planning/phases/01-premium-ui-foundations/01-CONTEXT.md
  </read_first>
  <action>
    Update frontend/tailwind.config.js to extend the theme with the new tokens and font families.
    
    1. Update colors.primary to use var(--brand-rausch).
    2. Add colors.primary-muted to use var(--brand-rausch-muted).
    3. Update borderRadius:
       - '3xl': 'var(--radius-container)'
       - 'xl': 'var(--radius-utility)'
       - 'lg': 'var(--radius-data)'
    4. Add boxShadow:
       - 'premium': 'var(--shadow-premium)'
    5. Add fontFamilies (ensure these are loaded in index.html if not already):
       - 'sans': ['Inter', 'ui-sans-serif', 'system-ui', ...]
       - 'serif': ['Playfair Display', 'ui-serif', 'Georgia', ...]
  </action>
  <verify>
    <automated>grep "var(--brand-rausch)" frontend/tailwind.config.js</automated>
  </verify>
  <done>Tailwind theme updated with new tokens</done>
  <acceptance_criteria>
    - frontend/tailwind.config.js maps primary to var(--brand-rausch)
    - frontend/tailwind.config.js defines 'premium' shadow
    - frontend/tailwind.config.js defines 'sans' and 'serif' font families
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build Pipeline | Tailwind configuration is static and processed during the build step. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | CSS Variables | accept | CSS variables are public by nature; no sensitive data is stored in them. |
</threat_model>

<verification>
Check a page that uses bg-primary to see the color change to Rausch.
</verification>

<success_criteria>
CSS variables and Tailwind theme correctly implement the Rausch palette and organic radii.
</success_criteria>

<output>
After completion, create .planning/phases/01-premium-ui-foundations/01-02-SUMMARY.md
</output>
