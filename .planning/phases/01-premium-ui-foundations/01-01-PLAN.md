---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/package.json", "frontend/vite.config.ts", "frontend/src/tests/setup.ts", "frontend/src/lib/motion.ts"]
autonomous: true
requirements: ["UI-01", "UI-04"]

must_haves:
  truths:
    - "framer-motion and vitest are installed in the frontend"
    - "Test environment is configured for frontend components"
    - "Premium animation constants are centralized"
  artifacts:
    - path: "frontend/src/lib/motion.ts"
      provides: "Shared animation constants (easing, duration)"
    - path: "frontend/src/tests/setup.ts"
      provides: "Vitest test environment setup"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/tests/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles:.*setup\\.ts"
---

<objective>
Prepare the project for premium UI development by installing necessary libraries, configuring testing, and establishing animation constants.

Purpose: Foundation for framer-motion orchestration and design token validation.
Output: Working test suite and centralized motion tokens.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-premium-ui-foundations/01-CONTEXT.md
@.planning/phases/01-premium-ui-foundations/01-RESEARCH.md
@.planning/phases/01-premium-ui-foundations/01-UI-SPEC.md
@.planning/phases/01-premium-ui-foundations/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Animation and Testing Dependencies</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json
    - .planning/phases/01-premium-ui-foundations/01-RESEARCH.md
  </read_first>
  <action>
    Install framer-motion for animations and vitest for component testing.
    Execute in project root:
    1. npm install framer-motion --prefix frontend
    2. npm install -D vitest @vitest/ui @testing-library/react @testing-library/jest-dom jsdom --prefix frontend
  </action>
  <verify>
    <automated>grep -E '"framer-motion"|"vitest"' frontend/package.json</automated>
  </verify>
  <done>Dependencies added to frontend/package.json</done>
  <acceptance_criteria>
    - frontend/package.json contains "framer-motion" in dependencies
    - frontend/package.json contains "vitest" in devDependencies
    - frontend/package.json contains "@testing-library/react" in devDependencies
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest Environment</name>
  <files>frontend/vite.config.ts, frontend/src/tests/setup.ts</files>
  <read_first>
    - frontend/vite.config.ts
    - .planning/phases/01-premium-ui-foundations/01-VALIDATION.md
  </read_first>
  <action>
    1. Create frontend/src/tests/setup.ts with:
       import '@testing-library/jest-dom';
    
    2. Update frontend/vite.config.ts to include Vitest configuration:
       import { defineConfig } from 'vitest/config'; (or merge with existing)
       Add 'test' property to defineConfig:
       test: {
         globals: true,
         environment: 'jsdom',
         setupFiles: './src/tests/setup.ts',
       }
  </action>
  <verify>
    <automated>grep "environment: 'jsdom'" frontend/vite.config.ts</automated>
  </verify>
  <done>Vitest configured and setup file created</done>
  <acceptance_criteria>
    - frontend/src/tests/setup.ts exists and contains import '@testing-library/jest-dom'
    - frontend/vite.config.ts contains test configuration with jsdom environment
    - frontend/vite.config.ts contains setupFiles pointing to ./src/tests/setup.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Establish Animation Constants</name>
  <files>frontend/src/lib/motion.ts</files>
  <read_first>
    - .planning/phases/01-premium-ui-foundations/01-UI-SPEC.md
  </read_first>
  <action>
    Create frontend/src/lib/motion.ts and export the following constants based on UI-SPEC:
    
    export const PREMIUM_EASE = [0.32, 0.72, 0, 1];
    export const PAGE_TRANSITION_DURATION = 0.4;
    export const MICRO_INTERACTION_DURATION = 0.25;
    
    export const PAGE_VARIANTS = {
      initial: { opacity: 0, y: 20 },
      animate: { 
        opacity: 1, 
        y: 0, 
        transition: { 
          duration: PAGE_TRANSITION_DURATION, 
          ease: PREMIUM_EASE 
        } 
      },
      exit: { 
        opacity: 0, 
        y: -20, 
        transition: { 
          duration: 0.3 
        } 
      }
    };
  </action>
  <verify>
    <automated>grep "PREMIUM_EASE" frontend/src/lib/motion.ts</automated>
  </verify>
  <done>Shared motion constants file created</done>
  <acceptance_criteria>
    - frontend/src/lib/motion.ts exists
    - Contains PREMIUM_EASE export with value [0.32, 0.72, 0, 1]
    - Contains PAGE_VARIANTS export with initial, animate, and exit states
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser Runtime | Framer motion executes in the user's browser; animations are client-side only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Framer Motion | mitigate | Centralize easing and duration to prevent inconsistent/heavy animation definitions that could thrash the main thread. |
</threat_model>

<verification>
Run 'npm test -- frontend' (once a test is added in later plans) to ensure environment is valid.
</verification>

<success_criteria>
Dependencies installed, testing environment configured, and motion primitives established in code.
</success_criteria>

<output>
After completion, create .planning/phases/01-premium-ui-foundations/01-01-SUMMARY.md
</output>
