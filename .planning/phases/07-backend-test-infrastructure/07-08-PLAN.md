---
phase: 07-backend-test-infrastructure
plan: 08
type: execute
wave: 8
depends_on: ["07-01"]
files_modified: [backend/tests/integration/misc_controllers.test.ts]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "System notifications are correctly triggered and retrieved"
    - "File uploads are handled and correctly stored/referenced"
    - "Service reviews and messages are correctly processed"
  artifacts:
    - path: "backend/tests/integration/misc_controllers.test.ts"
      provides: "Integration tests for notifications, uploads, reviews, and messaging"
  key_links:
    - from: "backend/tests/integration/misc_controllers.test.ts"
      to: "backend/src/controllers/notificationController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.get.*api/v1/notifications"
    - from: "backend/tests/integration/misc_controllers.test.ts"
      to: "backend/src/controllers/uploadController.ts"
      via: "supertest multi-part post"
      pattern: "request\\(app\\)\\.post.*api/v1/uploads"
---

<objective>
Verify auxiliary system features including notifications, file uploads, reviews, and messaging to ensure full platform functionality.

Purpose: Ensure secondary features are reliable and performant.
Output: Miscellaneous controllers integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@backend/src/controllers/notificationController.ts
@backend/src/controllers/uploadController.ts
@backend/src/controllers/reviewController.ts
@backend/src/controllers/messageController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notifications and Uploads Tests</name>
  <files>backend/tests/integration/misc_controllers.test.ts</files>
  <read_first>
    - backend/src/controllers/notificationController.ts
    - backend/src/controllers/uploadController.ts
  </read_first>
  <action>
    Implement notification and upload tests in `backend/tests/integration/misc_controllers.test.ts`:
    1. Notification Tests:
       - GET `/api/v1/notifications`: List notifications for user.
       - PATCH `/api/v1/notifications/:id/read`: Mark as read.
    2. Upload Tests:
       - POST `/api/v1/uploads`: Upload a sample image (using `attach()`).
       - Verify file metadata is returned and path is valid.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/misc_controllers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Notification lifecycle verified.
    - File upload functionality works with sample data.
  </acceptance_criteria>
  <done>Notification and upload integration tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Reviews and Messaging Tests</name>
  <files>backend/tests/integration/misc_controllers.test.ts</files>
  <read_first>
    - backend/src/controllers/reviewController.ts
    - backend/src/controllers/messageController.ts
  </read_first>
  <action>
    Add review and message tests to `backend/tests/integration/misc_controllers.test.ts`:
    1. Review Tests: POST/GET for service reviews.
    2. Message Tests: POST/GET for contact messages or chat.
    3. Verify that these records are correctly persisted in the database.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/misc_controllers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - User feedback (reviews/messages) flows are verified.
    - Basic CRUD for these entities works.
  </acceptance_criteria>
  <done>Review and messaging integration tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- User -> Upload API (File size/type validation)
- User -> Notification API

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | File Uploads | mitigate | Verify that upload API validates file types (SEC-10). |
| T-07-08-02 | Denial of Service | Uploads/Messages | mitigate | Verify that large payloads or rapid requests are handled gracefully (smoke test). |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/misc_controllers.test.ts`.
</verification>

<success_criteria>
- 80% coverage of notification, upload, review, and message controllers.
- Auxiliary features verified.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-08-SUMMARY.md`
</output>
