---
phase: 07-backend-test-infrastructure
plan: 05
type: execute
wave: 5
depends_on: ["07-01", "07-04"]
files_modified: [backend/tests/integration/payroll.test.ts]
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "Payroll periods can be created and locked"
    - "Commissions are correctly aggregated for the payroll period"
    - "Deductions (advances) are correctly applied to the final net pay"
    - "Locked periods cannot be modified or re-generated"
  artifacts:
    - path: "backend/tests/integration/payroll.test.ts"
      provides: "Integration tests for payroll generation and auditing"
  key_links:
    - from: "backend/tests/integration/payroll.test.ts"
      to: "backend/src/controllers/payrollController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.(post|get).*api/v1/payroll"
---

<objective>
Verify the payroll generation, aggregation, and locking logic to ensure financial integrity and prevent overpayment or duplication.

Purpose: Guarantee accurate compensation for staff and reliable financial records for the business.
Output: Payroll integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@.planning/phases/07-backend-test-infrastructure/07-04-PLAN.md
@backend/src/controllers/payrollController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payroll Generation and Calculation Tests</name>
  <files>backend/tests/integration/payroll.test.ts</files>
  <read_first>
    - backend/src/controllers/payrollController.ts
  </read_first>
  <action>
    Implement payroll generation tests in `backend/tests/integration/payroll.test.ts`:
    1. POST `/api/v1/payroll/generate`:
       - Successfully generate payroll for a given period.
       - Verify that commissions from the previous month are correctly fetched and divided by 4 (Rule check).
       - Verify that deductions/advances are subtracted.
       - Verify that `SystemLog` entry is created.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/payroll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Payroll calculation logic (Commissions/4 + Base - Deductions) is verified in a full integration context.
    - Audit logs are created.
  </acceptance_criteria>
  <done>Payroll generation integration tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Payroll Locking and Export Tests</name>
  <files>backend/tests/integration/payroll.test.ts</files>
  <read_first>
    - backend/src/controllers/payrollController.ts
  </read_first>
  <action>
    Implement payroll locking and export tests in `backend/tests/integration/payroll.test.ts`:
    1. POST `/api/v1/payroll/lock`:
       - Successfully lock a payroll period.
       - Verify that subsequent generate calls for the same period are rejected.
       - Verify that commissions in the period are marked as 'paid'.
    2. GET `/api/v1/payroll/export`:
       - Verify that the export endpoint returns valid Excel data (buffer).
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/payroll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Period locking is enforced and idempotent.
    - Export functionality returns binary data.
  </acceptance_criteria>
  <done>Payroll locking and export integration tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Manager -> Payroll API
- App -> Payroll Records

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Payroll Data | mitigate | Verify that only managers can generate or lock payroll (ASVS V13). |
| T-07-05-02 | Information Disclosure | Payroll Export | mitigate | Verify that payroll export requires manager authentication and doesn't leak data to unauthorized users. |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/payroll.test.ts`.
</verification>

<success_criteria>
- 80% coverage of payrollController.ts.
- Verification of the "Divide by 4" commission rule.
- Data consistency across generated and locked states.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-05-SUMMARY.md`
</output>
