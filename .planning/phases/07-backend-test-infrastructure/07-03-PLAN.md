---
phase: 07-backend-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["07-01"]
files_modified: [backend/tests/integration/auth.test.ts]
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "User can register with valid credentials"
    - "User cannot register with weak passwords (SEC-02)"
    - "User can login and receive valid JWT and refresh tokens"
    - "Refresh token rotation works correctly without race conditions (SEC-06)"
    - "Protected routes reject requests with invalid or expired tokens"
  artifacts:
    - path: "backend/tests/integration/auth.test.ts"
      provides: "Full integration test suite for authentication flows"
  key_links:
    - from: "backend/tests/integration/auth.test.ts"
      to: "backend/src/controllers/authController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.(post|get).*api/v1/auth"
---

<objective>
Verify the authentication system's integrity, security, and robustness through integration tests using Supertest and a real test database.

Purpose: Prevent regressions in user access control and protect against authentication bypasses.
Output: Auth integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@backend/src/controllers/authController.ts
@backend/src/middleware/authMiddleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Basic Flows (Register/Login)</name>
  <files>backend/tests/integration/auth.test.ts</files>
  <read_first>
    - backend/src/controllers/authController.ts (identify routes and payloads)
    - .planning/phases/03-security-hardening/03-02-SUMMARY.md (password complexity rules)
  </read_first>
  <action>
    Implement basic auth tests in `backend/tests/integration/auth.test.ts`:
    1. POST `/api/v1/auth/register`:
       - Success with valid payload.
       - Failure with weak password (V5 Validation).
       - Failure with duplicate email.
    2. POST `/api/v1/auth/login`:
       - Success returns 200 + token + refreshToken.
       - Failure with wrong password returns 401.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Registration validation works (success/failure cases).
    - Login flow works (success/failure cases).
  </acceptance_criteria>
  <done>Basic authentication integration tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Auth Advanced Security (Token Rotation/RBAC)</name>
  <files>backend/tests/integration/auth.test.ts</files>
  <read_first>
    - backend/src/controllers/authController.ts
  </read_first>
  <action>
    Add security-focused tests to `backend/tests/integration/auth.test.ts`:
    1. POST `/api/v1/auth/refresh`:
       - Success returns new tokens and invalidates old one (Rotation check - SEC-06).
       - Failure with reused or invalid refresh token.
    2. POST `/api/v1/auth/logout`:
       - Success invalidates the refresh token.
    3. RBAC check (V13):
       - Verify staff cannot access manager-only endpoints (e.g., a dummy protected route or existing manager endpoint).
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Token rotation is verified (old token becomes invalid).
    - Logout invalidates session.
    - RBAC boundaries are enforced.
  </acceptance_criteria>
  <done>Security-focused authentication integration tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Client -> Auth API (Untrusted input)
- App -> Refresh Token Store (Persistence)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Spoofing | /auth/login | mitigate | Verify that incorrect passwords consistently return 401 and don't leak account existence info. |
| T-07-03-02 | Elevation of Privilege | RBAC | mitigate | Integration tests must explicitly check that a 'staff' token cannot perform 'manager' actions (ASVS V13). |
| T-07-03-03 | Tampering | Refresh Tokens | mitigate | Verify token rotation (SEC-06) where using an old refresh token after rotation fails (ASVS V2). |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/auth.test.ts`.
</verification>

<success_criteria>
- 100% coverage of authController.ts methods.
- No hardcoded secrets used in tests.
- Database is clean after test suite completion.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-03-SUMMARY.md`
</output>
