---
phase: 07-backend-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/package.json, backend/.gitignore, backend/jest.config.ts, backend/tests/setup.ts, backend/tests/helpers/database.ts, backend/src/index.ts]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Jest runs and executes a sample test successfully"
    - "Server does not start a network listener during test runs"
    - "Database tables are truncated between test runs using a helper"
  artifacts:
    - path: "backend/jest.config.ts"
      provides: "Jest configuration with coverage thresholds"
    - path: "backend/tests/setup.ts"
      provides: "Global lifecycle hooks and environment loading"
    - path: "backend/tests/helpers/database.ts"
      provides: "Table truncation utility for clean test state"
  key_links:
    - from: "backend/src/index.ts"
      to: "process.env.NODE_ENV"
      via: "conditional listen guard"
---

<objective>
Establish the backend testing foundation using Jest and Supertest, ensuring a clean and isolated environment for all subsequent tests.

Purpose: Enable automated verification of backend correctness and security.
Output: Configured test runner, global setup, and database isolation utilities.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-backend-test-infrastructure/07-CONTEXT.md
@.planning/phases/07-backend-test-infrastructure/07-RESEARCH.md
@backend/package.json
@backend/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Update Gitignore</name>
  <files>backend/package.json, backend/.gitignore</files>
  <read_first>
    - backend/package.json (check devDependencies)
    - backend/.gitignore
  </read_first>
  <action>
    1. Install supertest and its types: `cd backend && npm install --save-dev supertest @types/supertest`.
    2. Add `coverage/` and `*.test-results.json` to `backend/.gitignore`.
    3. Ensure `npm test` script in `backend/package.json` is set to `jest --runInBand`.
  </action>
  <verify>
    <automated>cd backend && npm list supertest && grep "coverage/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `supertest` and `@types/supertest` are present in `backend/package.json` devDependencies.
    - `backend/.gitignore` contains `coverage/`.
    - `npm test` command exists and includes `--runInBand`.
  </acceptance_criteria>
  <done>Dependencies installed and gitignore updated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Jest and Test Environment</name>
  <files>backend/jest.config.ts, backend/tests/setup.ts, backend/tests/helpers/database.ts</files>
  <read_first>
    - .planning/phases/07-backend-test-infrastructure/07-RESEARCH.md (refer to code examples)
    - backend/src/utils/prisma.ts (check prisma export)
  </read_first>
  <action>
    1. Create `backend/jest.config.ts` per research (preset: ts-jest, testEnvironment: node, setupFilesAfterEnv: ['./tests/setup.ts'], coverageThreshold: 80%).
    2. Create `backend/tests/helpers/database.ts` with `truncateAllTables` function using `prisma.$queryRaw` to find all tables and `prisma.$executeRawUnsafe` to TRUNCATE CASCADE.
    3. Create `backend/tests/setup.ts` to:
       - Load `.env.test` using `dotenv`.
       - Fail if `DATABASE_URL` doesn't contain "test" (Safety check per D-01).
       - Run `truncateAllTables()` in `beforeEach`.
       - Call `prisma.$disconnect()` in `afterAll`.
    4. Create a dummy test `backend/tests/smoke.test.ts` with `test('smoke', () => expect(1).toBe(1))`.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jest.config.ts` exists and specifies `tests/setup.ts`.
    - `tests/setup.ts` exists and loads environment variables.
    - `tests/helpers/database.ts` exists with truncation logic.
    - Smoke test passes.
  </acceptance_criteria>
  <done>Jest infrastructure configured and verified with a smoke test.</done>
</task>

<task type="auto">
  <name>Task 3: Guard Server Listen and Export App</name>
  <files>backend/src/index.ts</files>
  <read_first>
    - backend/src/index.ts (find app.listen)
  </read_first>
  <action>
    Modify `backend/src/index.ts` to ensure the server doesn't start listening during tests:
    Update the listen guard to: `if (process.env.NODE_ENV !== 'test' && (process.env.NODE_ENV !== 'production' || !process.env.VERCEL)) { ... }`.
    This prevents port conflicts when Supertest imports the app.
  </action>
  <verify>
    <automated>grep "process.env.NODE_ENV !== 'test'" backend/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/index.ts` has a check for `process.env.NODE_ENV !== 'test'` before calling `app.listen`.
    - `app` is exported from `backend/src/index.ts`.
  </acceptance_criteria>
  <done>Server listener guarded against test environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Test Code -> Test Database (Isolated from production)
- Supertest -> App Instance (In-process integration)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Test Database | mitigate | Verify `DATABASE_URL` contains "test" in `setup.ts` to prevent accidental truncation of production data. |
</threat_model>

<verification>
Run `npm test --prefix backend` and ensure all tests (including smoke test) pass.
</verification>

<success_criteria>
- `npm test` executes without errors.
- Test coverage report can be generated.
- Production database is safe from test interference.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-01-SUMMARY.md`
</output>
