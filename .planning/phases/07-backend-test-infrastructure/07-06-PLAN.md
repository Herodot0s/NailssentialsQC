---
phase: 07-backend-test-infrastructure
plan: 06
type: execute
wave: 6
depends_on: ["07-01"]
files_modified: [backend/tests/integration/staff_attendance.test.ts]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Staff members can be created, updated, and listed"
    - "Staff attendance (clock-in/out) is correctly recorded"
    - "Attendance status (on-time, late, absent) is correctly determined"
  artifacts:
    - path: "backend/tests/integration/staff_attendance.test.ts"
      provides: "Integration tests for staff management and attendance tracking"
  key_links:
    - from: "backend/tests/integration/staff_attendance.test.ts"
      to: "backend/src/controllers/staffController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.(post|get).*api/v1/staff"
    - from: "backend/tests/integration/staff_attendance.test.ts"
      to: "backend/src/controllers/attendanceController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.post.*api/v1/attendance"
---

<objective>
Verify staff management and attendance tracking functionality to ensure operational data integrity.

Purpose: Protect staff records and ensure accurate attendance data for payroll and scheduling.
Output: Staff and Attendance integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@backend/src/controllers/staffController.ts
@backend/src/controllers/attendanceController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staff Management Integration Tests</name>
  <files>backend/tests/integration/staff_attendance.test.ts</files>
  <read_first>
    - backend/src/controllers/staffController.ts
  </read_first>
  <action>
    Implement staff management tests in `backend/tests/integration/staff_attendance.test.ts`:
    1. POST `/api/v1/staff`: Create a new staff member.
    2. GET `/api/v1/staff`: List all staff.
    3. GET `/api/v1/staff/:id`: Get staff details.
    4. PATCH `/api/v1/staff/:id`: Update staff information (e.g., base salary).
    5. DELETE `/api/v1/staff/:id`: Archive/Delete staff member.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/staff_attendance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full CRUD for staff is verified via API.
    - RBAC is enforced (only managers can modify staff).
  </acceptance_criteria>
  <done>Staff management integration tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Attendance Tracking Integration Tests</name>
  <files>backend/tests/integration/staff_attendance.test.ts</files>
  <read_first>
    - backend/src/controllers/attendanceController.ts
  </read_first>
  <action>
    Add attendance tests to `backend/tests/integration/staff_attendance.test.ts`:
    1. POST `/api/v1/attendance/clock-in`: Verify clock-in timestamp and status.
    2. POST `/api/v1/attendance/clock-out`: Verify clock-out and duration calculation.
    3. GET `/api/v1/attendance`: List attendance records for a period.
    4. GET `/api/v1/attendance/stats`: Verify aggregation logic for attendance reports.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/staff_attendance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Clock-in/out flow is verified.
    - Attendance status logic (late/on-time) is smoke tested.
  </acceptance_criteria>
  <done>Attendance tracking integration tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Staff -> Attendance API (Identity verification)
- Manager -> Staff API

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Spoofing | Clock-in | mitigate | Verify that a staff member can only clock in for themselves (ASVS V13). |
| T-07-06-02 | Tampering | Attendance Records | mitigate | Verify that attendance records cannot be modified by staff after creation. |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/staff_attendance.test.ts`.
</verification>

<success_criteria>
- 80% coverage of staffController.ts and attendanceController.ts.
- Staff lifecycle and attendance flows verified.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-06-SUMMARY.md`
</output>
