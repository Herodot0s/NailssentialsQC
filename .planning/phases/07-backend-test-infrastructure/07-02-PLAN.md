---
phase: 07-backend-test-infrastructure
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified: [backend/src/utils/commissionLogic.ts, backend/src/controllers/appointmentCompletion.ts, backend/tests/unit/commission.test.ts]
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Commission calculation logic is extracted into a testable pure function"
    - "Tiered rates (5%, 8%, 10%) are correctly calculated based on last month sales"
    - "Specialty quota (20%) overrides base tiered rates when staff sales exceed ₱6,000"
  artifacts:
    - path: "backend/src/utils/commissionLogic.ts"
      provides: "Pure functions for commission calculation"
    - path: "backend/tests/unit/commission.test.ts"
      provides: "Unit tests covering all rate boundaries and quota scenarios"
  key_links:
    - from: "backend/tests/unit/commission.test.ts"
      to: "backend/src/utils/commissionLogic.ts"
      via: "unit test imports"
      pattern: "import.*getCommissionRate.*from.*utils/commissionLogic"
    - from: "backend/src/controllers/appointmentCompletion.ts"
      to: "backend/src/utils/commissionLogic.ts"
      via: "utility function call"
      pattern: "getCommissionRate\\("
---

<objective>
Extract commission calculation logic into a pure, testable utility and verify its correctness through comprehensive unit tests.

Purpose: Ensure financial accuracy for staff commissions without requiring a database for core logic verification.
Output: Refactored controller and new unit test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@backend/src/controllers/appointmentCompletion.ts
</context>

<feature>
  <name>Commission Calculation Logic</name>
  <files>backend/src/utils/commissionLogic.ts, backend/tests/unit/commission.test.ts</files>
  <behavior>
    - Tier 1: &lt; ₱51,000 last month sales -> 5% rate
    - Tier 2: ₱51,000 to ₱54,999 last month sales -> 8% rate
    - Tier 3: >= ₱55,000 last month sales -> 10% rate
    - Specialty Quota: Staff sales >= ₱6,000 in current month -> 20% rate (overrides tiers)
    - Calculation: baseAmount * rate = commissionAmount
  </behavior>
  <implementation>
    1. Extract the logic from `getTieredCommissionRate` and `checkSpecialtyQuota` in `appointmentCompletion.ts`.
    2. Create `backend/src/utils/commissionLogic.ts` with a function `getCommissionRate(lastMonthSales: number, currentMonthStaffSales: number): number`.
    3. Update `backend/src/controllers/appointmentCompletion.ts` to use this new utility (keeping DB fetching logic in the controller/helpers but moving calculation to the pure function).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Extract Commission Logic and Write Tests</name>
  <files>backend/src/utils/commissionLogic.ts, backend/tests/unit/commission.test.ts</files>
  <read_first>
    - backend/src/controllers/appointmentCompletion.ts (extract logic)
  </read_first>
  <action>
    1. Create `backend/src/utils/commissionLogic.ts` exporting `getCommissionRate`.
    2. Create `backend/tests/unit/commission.test.ts` with test cases for:
       - 5% rate: `getCommissionRate(50000, 0) === 0.05`
       - 8% rate: `getCommissionRate(51000, 0) === 0.08`
       - 10% rate: `getCommissionRate(55000, 0) === 0.10`
       - 20% rate (quota): `getCommissionRate(50000, 6000) === 0.20`
       - 20% rate (quota override): `getCommissionRate(60000, 6000) === 0.20`
    3. Ensure tests pass before refactoring the controller.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/unit/commission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getCommissionRate` correctly identifies all tiers.
    - Specialty quota correctly overrides tiered rates.
    - All unit tests pass.
  </acceptance_criteria>
  <done>Commission logic extracted and unit tested.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Appointment Controller</name>
  <files>backend/src/controllers/appointmentCompletion.ts</files>
  <read_first>
    - backend/src/utils/commissionLogic.ts
    - backend/src/controllers/appointmentCompletion.ts
  </read_first>
  <action>
    Update `backend/src/controllers/appointmentCompletion.ts` to import and use `getCommissionRate`.
    The controller should still fetch the sales figures from the DB, but use the utility for the rate determination.
  </action>
  <verify>
    <automated>grep "getCommissionRate" backend/src/controllers/appointmentCompletion.ts</automated>
  </verify>
  <acceptance_criteria>
    - Controller code is cleaner and delegates rate calculation to the utility.
    - Compilation passes.
  </acceptance_criteria>
  <done>Controller refactored to use testable commission logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Utility Logic -> Financial Calculations

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | commissionLogic.ts | accept | No PII involved, pure math. |
| T-07-02-02 | Tampering | Financial Logic | mitigate | Use unit tests to verify boundary conditions (e.g., exactly ₱51,000) are handled correctly. |
</threat_model>

<verification>
Run `npm test --prefix backend tests/unit/commission.test.ts`.
</verification>

<success_criteria>
- Commission calculation logic is covered by 100% unit test branch coverage.
- Appointment completion controller uses the shared utility.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-02-SUMMARY.md`
</output>
