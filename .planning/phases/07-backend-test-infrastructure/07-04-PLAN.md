---
phase: 07-backend-test-infrastructure
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02"]
files_modified: [backend/tests/integration/appointments.test.ts]
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "Appointments can be created with valid staff, service, and customer data"
    - "Appointment completion calculates commissions correctly (integrating with 07-02)"
    - "Transaction and commissions are created atomically (PERF-05)"
    - "SystemLog entries are created for completions (FEAT-01)"
  artifacts:
    - path: "backend/tests/integration/appointments.test.ts"
      provides: "Integration tests for the appointment lifecycle"
---

<objective>
Verify the appointment lifecycle (booking and completion) and ensure that business rules, transactions, and audit logs are correctly handled.

Purpose: Protect the core revenue-generating workflow and commission accuracy.
Output: Appointment integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@.planning/phases/07-backend-test-infrastructure/07-02-PLAN.md
@backend/src/controllers/appointmentController.ts
@backend/src/controllers/appointmentCompletion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Appointment Integration Tests</name>
  <files>backend/tests/integration/appointments.test.ts</files>
  <read_first>
    - backend/src/controllers/appointmentCompletion.ts
    - backend/src/controllers/appointmentController.ts
  </read_first>
  <action>
    Create `backend/tests/integration/appointments.test.ts` covering:
    1. POST `/api/v1/appointments`:
       - Successfully create an appointment with multiple items.
       - Verify validation (V5) for dates and staff/service IDs.
    2. POST `/api/v1/appointments/:id/complete`:
       - Successfully complete appointment.
       - Verify `transaction` is created with correct receipt number format.
       - Verify `commission` records are created for each item.
       - Verify `system_log` entry is created (FEAT-01).
       - Verify atomicity: Mock a failure in one part and ensure no partial data remains (rollback test).
    3. Availability checks:
       - Verify staff availability logic returns correct slots.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/appointments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Appointment creation and completion flows are verified.
    - Commission amounts in DB match expected values from `commissionLogic.ts`.
    - Transactions are correctly recorded.
  </acceptance_criteria>
  <done>Appointment integration tests complete and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Client -> Appointment API
- App -> Payment/Transaction Records

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | Commission Records | mitigate | Use integration tests to verify that `commission_rate` and `amount` are calculated by the server and cannot be influenced by client input (ASVS V13). |
| T-07-04-02 | Elevation of Privilege | Appointment Access | mitigate | Verify that a customer can only view/cancel their own appointments (ASVS V13). |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/appointments.test.ts`.
</verification>

<success_criteria>
- 80% coverage of appointment-related controllers.
- Verification of atomic transaction behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-04-SUMMARY.md`
</output>
