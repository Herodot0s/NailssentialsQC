---
phase: 07-backend-test-infrastructure
plan: 07
type: execute
wave: 7
depends_on: ["07-01"]
files_modified: [backend/tests/integration/catalog_reports.test.ts]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Services and Service Categories can be managed (CRUD)"
    - "Customer profiles can be created and tracked"
    - "Reports generate correct summary data for sales and staff performance"
  artifacts:
    - path: "backend/tests/integration/catalog_reports.test.ts"
      provides: "Integration tests for services, customers, and reporting"
  key_links:
    - from: "backend/tests/integration/catalog_reports.test.ts"
      to: "backend/src/controllers/serviceController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.(post|get).*api/v1/services"
    - from: "backend/tests/integration/catalog_reports.test.ts"
      to: "backend/src/controllers/reportController.ts"
      via: "supertest request calls"
      pattern: "request\\(app\\)\\.get.*api/v1/reports"
---

<objective>
Verify the service catalog, customer management, and reporting modules to ensure data accuracy and catalog integrity.

Purpose: Protect product information and ensure reliable business intelligence via reports.
Output: Service, Customer, and Report integration test suite.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-backend-test-infrastructure/07-01-PLAN.md
@backend/src/controllers/serviceController.ts
@backend/src/controllers/customerController.ts
@backend/src/controllers/reportController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service and Customer Management Tests</name>
  <files>backend/tests/integration/catalog_reports.test.ts</files>
  <read_first>
    - backend/src/controllers/serviceController.ts
    - backend/src/controllers/customerController.ts
  </read_first>
  <action>
    Implement catalog and customer tests in `backend/tests/integration/catalog_reports.test.ts`:
    1. Service CRUD: POST/GET/PATCH/DELETE for services and categories.
    2. Customer CRUD: POST/GET/PATCH/DELETE for customer profiles.
    3. Verify validation (V5) for required fields and price formats.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/catalog_reports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Service and customer lifecycle verified via API.
    - Public access to services list is confirmed.
  </acceptance_criteria>
  <done>Service and customer integration tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Reporting and Analytics Tests</name>
  <files>backend/tests/integration/catalog_reports.test.ts</files>
  <read_first>
    - backend/src/controllers/reportController.ts
  </read_first>
  <action>
    Add reporting tests to `backend/tests/integration/catalog_reports.test.ts`:
    1. GET `/api/v1/reports/sales`: Verify sales aggregation (Daily/Weekly/Monthly).
    2. GET `/api/v1/reports/staff-performance`: Verify commission and service counts per staff.
    3. GET `/api/v1/reports/service-popularity`: Verify most booked services.
    4. Verify that reports return correct numeric values based on test data fixtures.
  </action>
  <verify>
    <automated>cd backend && npx jest tests/integration/catalog_reports.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Report data matches expectations for provided test dataset.
    - RBAC is enforced (manager only for reports).
  </acceptance_criteria>
  <done>Reporting integration tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Manager -> Report API (Sensitive business data)
- Public -> Service List

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Information Disclosure | Reports | mitigate | Verify that only managers can access financial and performance reports. |
</threat_model>

<verification>
Run `npm test --prefix backend tests/integration/catalog_reports.test.ts`.
</verification>

<success_criteria>
- 80% coverage of serviceController.ts, customerController.ts, and reportController.ts.
- Catalog integrity and reporting accuracy verified.
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-test-infrastructure/07-07-SUMMARY.md`
</output>
