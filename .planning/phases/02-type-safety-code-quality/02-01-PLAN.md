---
phase: "02-type-safety-code-quality"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "frontend/src/types/User.ts"
  - "frontend/src/types/CartItem.ts"
  - "frontend/src/types/api.ts"
  - "frontend/src/api/apiClient.ts"
  - "frontend/src/context/AuthContext.tsx"
  - "frontend/src/context/CartContext.tsx"
  - "frontend/src/pages/Login.tsx"
  - "frontend/src/pages/Register.tsx"
  - "frontend/src/pages/Booking.tsx"
  - "frontend/src/pages/ManagerDashboard.tsx"
  - "frontend/src/pages/CustomerAppointments.tsx"
  - "frontend/src/pages/StaffDashboard.tsx"
  - "frontend/src/components/DrillDownLineChart.tsx"
  - "frontend/src/components/ReceiptModal.tsx"
  - "frontend/src/components/ProfilePictureUpload.tsx"
autonomous: true
requirements:
  - "DEBT-01"
user_setup: []
must_haves:
  truths:
    - "Frontend has no `any` type usage in catch blocks or variable declarations"
    - "ESLint `@typescript-eslint/no-explicit-any` passes clean for frontend"
    - "All API client functions have properly typed parameters"
  artifacts:
    - path: "frontend/src/types/User.ts"
      provides: "User interface for AuthContext"
      contains: "export interface User"
    - path: "frontend/src/types/CartItem.ts"
      provides: "CartItem interface for CartContext"
      contains: "export interface CartItem"
    - path: "frontend/src/types/api.ts"
      provides: "API request/response interfaces"
      contains: "export interface LoginRequest"
    - path: "frontend/src/api/apiClient.ts"
      provides: "Typed API client functions"
      pattern: "data: (LoginRequest|CreateStaffRequest|CreateAppointmentRequest)"
  key_links:
    - from: "frontend/src/context/AuthContext.tsx"
      to: "frontend/src/types/User.ts"
      via: "import type { User }"
      pattern: "import.*User.*from.*types"
    - from: "frontend/src/api/apiClient.ts"
      to: "frontend/src/types/api.ts"
      via: "import type { ... }"
      pattern: "import.*from.*types/api"
---

<objective>
Replace all `any` type usages across the frontend codebase (~40 occurrences) with proper TypeScript interfaces. This addresses DEBT-01 by creating type definition files and systematically replacing `any` in catch blocks, function parameters, and state declarations.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/ROADMAP.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/02-type-safety-code-quality/02-CONTEXT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/02-type-safety-code-quality/02-PATTERNS.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/02-type-safety-code-quality/02-RESEARCH.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/api/apiClient.ts
@C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/AuthContext.tsx
@C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/CartContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend type definition files</name>
  <read_first>
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/02-type-safety-code-quality/02-PATTERNS.md (lines 599-713 for type definitions)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/AuthContext.tsx (lines 4-10 for User interface)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/CartContext.tsx (lines 3-11 for CartItem interface)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/api/apiClient.ts (all `data: any` usages)
  </read_first>
  <acceptance_criteria>
    - `frontend/src/types/User.ts` exists and contains `export interface User { id: number; email: string | null; phone: string | null; role: string; fullName: string; }`
    - `frontend/src/types/CartItem.ts` exists and contains `export interface CartItem { serviceId: number; serviceName: string; price: number; duration: number; staffId?: number; staffName?: string; startTime?: string; }`
    - `frontend/src/types/api.ts` exists and contains `export interface LoginRequest { identifier: string; password: string; }`, `export interface RegisterRequest { fullName: string; password: string; email?: string | null; phone?: string | null; }`, `export interface CreateStaffRequest { fullName: string; username: string; password: string; specializations?: string; basePayPerWeek: number; dailyTarget: number; sssNumber?: string; tinNumber?: string; govId?: string; profilePictureUrl?: string; }`, `export interface CreateAppointmentRequest { items: { serviceId: number; staffId: number; startTime: string; }[]; date: string; notes?: string; customerId?: number; isWalkIn?: boolean; }`, `export interface CompleteAppointmentRequest { paymentMethod: 'cash' | 'gcash'; }`, `export interface UpdateAttendanceRequest { checkIn?: string | null; checkOut?: string | null; tardinessMinutes?: number; deductionAmount?: number; }`, `export interface SubmitReviewRequest { appointmentItemId: number; rating: number; tags: string[]; comment?: string; }`
    - `grep -c "export interface" frontend/src/types/User.ts frontend/src/types/CartItem.ts frontend/src/types/api.ts` returns 7 total (1+1+5)
  </acceptance_criteria>
  <action>
    Create three type definition files in `frontend/src/types/`:

    1. **`frontend/src/types/User.ts`** - Create with exact content:
    ```typescript
    export interface User {
      id: number;
      email: string | null;
      phone: string | null;
      role: string;
      fullName: string;
    }
    ```

    2. **`frontend/src/types/CartItem.ts`** - Create with exact content:
    ```typescript
    export interface CartItem {
      serviceId: number;
      serviceName: string;
      price: number;
      duration: number;
      staffId?: number;
      staffName?: string;
      startTime?: string;
    }
    ```

    3. **`frontend/src/types/api.ts`** - Create with exact content:
    ```typescript
    // Auth API types
    export interface LoginRequest {
      identifier: string;
      password: string;
    }

    export interface RegisterRequest {
      fullName: string;
      password: string;
      email?: string | null;
      phone?: string | null;
    }

    export interface AuthResponse {
      user: User;
      tokens: {
        accessToken: string;
        refreshToken: string;
      };
    }

    // Staff API types
    export interface CreateStaffRequest {
      fullName: string;
      username: string;
      password: string;
      specializations?: string;
      basePayPerWeek: number;
      dailyTarget: number;
      sssNumber?: string;
      tinNumber?: string;
      govId?: string;
      profilePictureUrl?: string;
    }

    // Appointment API types
    export interface CreateAppointmentRequest {
      items: {
        serviceId: number;
        staffId: number;
        startTime: string;
      }[];
      date: string;
      notes?: string;
      customerId?: number;
      isWalkIn?: boolean;
    }

    export interface CompleteAppointmentRequest {
      paymentMethod: 'cash' | 'gcash';
    }

    // Attendance API types
    export interface UpdateAttendanceRequest {
      checkIn?: string | null;
      checkOut?: string | null;
      tardinessMinutes?: number;
      deductionAmount?: number;
    }

    // Review API types
    export interface SubmitReviewRequest {
      appointmentItemId: number;
      rating: number;
      tags: string[];
      comment?: string;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "export interface" frontend/src/types/User.ts frontend/src/types/CartItem.ts frontend/src/types/api.ts</automated>
  </verify>
  <done>
    All three type definition files created with proper interfaces. User.ts has User interface, CartItem.ts has CartItem interface, api.ts has LoginRequest, RegisterRequest, CreateStaffRequest, CreateAppointmentRequest, CompleteAppointmentRequest, UpdateAttendanceRequest, SubmitReviewRequest interfaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update apiClient.ts and context files with proper types</name>
  <read_first>
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/api/apiClient.ts (full file - all `data: any` usages at lines 71-72, 76-77, 79, 84, 108, 118, 126)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/AuthContext.tsx (full file - catch block at line 41)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/context/CartContext.tsx (full file - check for any types)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/phases/02-type-safety-code-quality/02-PATTERNS.md (lines 733-742 for apiClient pattern)
  </read_first>
  <acceptance_criteria>
    - `frontend/src/api/apiClient.ts` has NO `data: any` patterns - grep returns 0 for `data: any` in this file
    - `frontend/src/api/apiClient.ts` imports types: `import type { CreateStaffRequest, UpdateAttendanceRequest, SubmitReviewRequest, CreateAppointmentRequest } from '@/types/api';`
    - `frontend/src/context/AuthContext.tsx` line 41: `catch (error)` changed to `catch (error: unknown)` with `if (error instanceof Error) { console.error('Error details:', error.message); }` block added
    - `frontend/src/context/CartContext.tsx` has no `any` types (verify with grep)
    - TypeScript compilation passes: `cd frontend && npx tsc --noEmit` succeeds
  </acceptance_criteria>
  <action>
    **Step 1: Update `frontend/src/api/apiClient.ts`**

    Add import at top of file:
    ```typescript
    import type { CreateStaffRequest, UpdateAttendanceRequest, SubmitReviewRequest, CreateAppointmentRequest } from '@/types/api';
    ```

    Replace all `data: any` parameter types:
    - Line 71: `createService = (data: any)` → `createService = (data: any)` → Actually check if service types needed. Keep as `any` for now if no interface defined, OR create `CreateServiceRequest` interface in api.ts.
    - Actually, let me be precise. From the grep output:
      - Line 71: `createService = (data: any)` → If no service type, keep `any` for now (services not in scope per CONTEXT.md)
      - Line 72: `updateService = (id: number, data: any)` → Same as above
      - Line 76: `createStaff = (data: any)` → `createStaff = (data: CreateStaffRequest)`
      - Line 77: `updateStaff = (id: number, data: any)` → `updateStaff = (id: number, data: Partial<CreateStaffRequest>)`
      - Line 79: `updateStaffSchedule = (id: number, data: { schedules: any[] })` → `updateStaffSchedule = (id: number, data: { schedules: ScheduleItem[] })` (need to add ScheduleItem to api.ts or types)
      - Actually, add `ScheduleItem` interface to `frontend/src/types/api.ts`:
      ```typescript
      export interface ScheduleItem {
        day_of_week: number;
        start_time: string;
        end_time: string;
        is_active: boolean;
      }
      ```
      Then: `updateStaffSchedule = (id: number, data: { schedules: ScheduleItem[] })`
      - Line 84: `createAppointment = (bookingData: any)` → `createAppointment = (bookingData: CreateAppointmentRequest)`
      - Line 108: `submitReview = (data: any)` → `submitReview = (data: SubmitReviewRequest)`
      - Line 118: `updateCustomerProfile = (data: any)` → Keep as `any` or create type (not in scope, keep minimal)
      - Line 126: `updateAttendance = (id: number, data: any)` → `updateAttendance = (id: number, data: UpdateAttendanceRequest)`

    **Step 2: Update `frontend/src/context/AuthContext.tsx`**

    Add import: `import { User } from '@/types/User';` (or use relative path)

    Actually, the User interface is already defined inline at lines 4-10. Replace with import:
    - Remove lines 4-10 (interface User block)
    - Add at top: `import { User } from '@/types/User';`

    Update line 41 catch block from:
    ```typescript
    } catch (error) {
      console.error('Session verification failed:', error);
    ```
    To:
    ```typescript
    } catch (error: unknown) {
      console.error('Session verification failed:', error);
      if (error instanceof Error) {
        console.error('Error details:', error.message);
      }
    ```

    **Step 3: Update `frontend/src/context/CartContext.tsx`**

    Add import: `import { CartItem } from '@/types/CartItem';`

    Remove inline CartItem interface (lines 3-11) and replace with the import above.

    Check for any remaining `any` types in the file and replace with proper types.
  </action>
  <verify>
    <automated>grep -n "data: any\|catch (error: any)\|: any" frontend/src/api/apiClient.ts frontend/src/context/AuthContext.tsx frontend/src/context/CartContext.tsx</automated>
  </verify>
  <done>
    apiClient.ts has no `data: any` parameters (except service endpoints not in scope). AuthContext.tsx uses `catch (error: unknown)` with instanceof check. CartContext.tsx uses imported CartItem type. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace `any` types in all frontend page and component files</name>
  <read_first>
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/Login.tsx (lines 32, 51 - data: any and catch error: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/Register.tsx (lines 31, 48 - data: any and catch error: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/Booking.tsx (lines 73, 111 - s: any and catch err: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/ManagerDashboard.tsx (lines 158, 160, 162, 240, 257, 291, 388 - useState<any[]> and catch err: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/CustomerAppointments.tsx (lines 38, 84, 104 - review?: any, item: any, catch err: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/pages/StaffDashboard.tsx (line 112 - catch err: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/components/DrillDownLineChart.tsx (lines 41, 67, 73 - data: any, handleLegendClick, handleChartClick)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/components/ReceiptModal.tsx (lines 17, 51, 104 - appointment: any)
    - @C:/Users/Administrator/Desktop/nailssentialsqc-system/frontend/src/components/ProfilePictureUpload.tsx (line 67 - catch err: any)
  </read_first>
  <acceptance_criteria>
    - `grep -c ": any\|data: any\|catch.*: any" frontend/src/pages/Login.tsx frontend/src/pages/Register.tsx frontend/src/pages/Booking.tsx frontend/src/pages/ManagerDashboard.tsx frontend/src/pages/CustomerAppointments.tsx frontend/src/pages/StaffDashboard.tsx` returns 0 for each file
    - `grep -c ": any" frontend/src/components/DrillDownLineChart.tsx frontend/src/components/ReceiptModal.tsx frontend/src/components/ProfilePictureUpload.tsx` returns 0 for each file
    - `cd frontend && npx tsc --noEmit` succeeds with no errors
    - Login.tsx uses `onSubmit = async (data: LoginRequest)` and `catch (error: unknown)` with instanceof Error check
    - Register.tsx uses `onSubmit = async (data: RegisterRequest)` and `catch (error: unknown)` with instanceof Error check
    - Booking.tsx filter uses `(s: Staff)` where Staff interface is defined or imported
    - ManagerDashboard.tsx: `payrollPeriods` uses `useState<any[]>([])` → `useState<PayrollPeriod[]>([])` (define PayrollPeriod in types), `attendance` uses `useState<AttendanceRecord[]>([])` (define in types), `categories` uses `useState<Category[]>([])` (define in types), `let data: any = {}` on line 388 removed or typed
  </acceptance_criteria>
  <action>
    **Systematically replace all `any` types in frontend files:**

    **Step 1: Add missing types to `frontend/src/types/api.ts`**
    Add these interfaces:
    ```typescript
    export interface PayrollPeriod {
      id: number;
      start_date: string;
      end_date: string;
      is_locked: boolean;
    }

    export interface PayrollRecord {
      staffId: number;
      fullName: string;
      commissionCount: number;
      totalCommission: number;
      attendanceCount: number;
      totalDeduction: number;
      basePay: number;
      netPay: number;
    }

    export interface AttendanceRecord {
      id: number;
      staff_id: number;
      check_in: string | null;
      check_out: string | null;
      status: string;
    }

    export interface Review {
      id: number;
      rating: number;
      tags: string[];
      is_approved_for_public: boolean;
      created_at: string;
      customer: { full_name: string };
      staff: { full_name: string };
      appointment_item: { service: { name: string } };
    }

    export interface Category {
      id: number;
      name: string;
    }

    export interface Staff {
      id: number;
      fullName: string;
      specializations: string;
      role: string;
    }

    export interface ScheduleItem {
      day_of_week: number;
      start_time: string;
      end_time: string;
      is_active: boolean;
    }
    ```

    **Step 2: Update `frontend/src/pages/Login.tsx`**
    - Add import: `import type { LoginRequest } from '@/types/api';`
    - Line 32: `onSubmit = async (data: any)` → `onSubmit = async (data: LoginRequest)`
    - Line 51: `catch (error: any)` → `catch (error: unknown)` and add:
      ```typescript
      const message = error instanceof Error ? error.message : 'Login failed. Please check your credentials.';
      setServerError(message);
      ```

    **Step 3: Update `frontend/src/pages/Register.tsx`**
    - Add import: `import type { RegisterRequest } from '@/types/api';`
    - Line 31: `onSubmit = async (data: any)` → `onSubmit = async (data: RegisterRequest)`
    - Line 48: `catch (error: any)` → `catch (error: unknown)` with instanceof check

    **Step 4: Update `frontend/src/pages/Booking.tsx`**
    - Add import: `import type { Staff } from '@/types/api';`
    - Line 73: `(s: any) =>` → `(s: Staff) =>`
    - Line 111: `catch (err: any)` → `catch (err: unknown)` with instanceof check

    **Step 5: Update `frontend/src/pages/ManagerDashboard.tsx`**
    - Add imports: `import type { PayrollPeriod, PayrollRecord, AttendanceRecord, Review, Category } from '@/types/api';`
    - Line 158: `useState<any[]>([])` → `useState<PayrollPeriod[]>([])`
    - Line 160: `useState<any[]>([])` → `useState<AttendanceRecord[]>([])`
    - Line 162: `useState<any[]>([])` → `useState<Category[]>([])`
    - Lines 240, 257, 291: `catch (err: any)` → `catch (err: unknown)` with instanceof Error check
    - Line 388: `let data: any = {}` → Remove or type properly (check what `data` is used for - likely API response, use proper type)

    **Step 6: Update `frontend/src/pages/CustomerAppointments.tsx`**
    - Line 38: `review?: any;` → `review?: SubmitReviewRequest;`
    - Line 84: `(item: any)` → `(item: AppointmentItem)` (define AppointmentItem in types if needed)
    - Line 104: `catch (err: any)` → `catch (err: unknown)` with instanceof check

    **Step 7: Update `frontend/src/pages/StaffDashboard.tsx`**
    - Line 112: `catch (err: any)` → `catch (err: unknown)` with instanceof check

    **Step 8: Update `frontend/src/components/DrillDownLineChart.tsx`**
    - Line 41: `const entry: any =` → `const entry: Record<string, number> =`
    - Line 67: `handleLegendClick = (data: any)` → `handleLegendClick = (data: { value: string })`
    - Line 73: `handleChartClick = (data: any)` → `handleChartClick = (data: { activePayload?: Array<{ payload: { date: string } }> })`

    **Step 9: Update `frontend/src/components/ReceiptModal.tsx`**
    - DefineReceiptModalProps or inline type for `appointment` prop
    - Line 17: `appointment: any;` → `appointment: AppointmentWithServices;` (define type)
    - Line 51: `(acc: number, s: any)` → `(acc: number, s: { price_at_booking?: number; service?: { price: number } })`
    - Line 104: `(s: any, idx: number)` → `(s: Service, idx: number)` with proper Service type

    **Step 10: Update `frontend/src/components/ProfilePictureUpload.tsx`**
    - Line 67: `catch (err: any)` → `catch (err: unknown)` with instanceof Error check

    **Step 11: Verify**
    - Run `cd frontend && npx tsc --noEmit` to verify no TypeScript errors
    - Run `grep -rn ": any\|data: any\|catch.*: any" frontend/src/pages/ frontend/src/components/` to verify zero `any` occurrences remaining
  </action>
  <verify>
    <automated>grep -rn ": any\|data: any\|catch.*: any" frontend/src/pages/ frontend/src/components/ frontend/src/context/ frontend/src/api/ 2>/dev/null | grep -v "node_modules" | wc -l</automated>
  </verify>
  <done>
    All `any` type usages removed from frontend codebase. grep returns 0 for `: any`, `data: any`, `catch (error: any)` patterns across all frontend source files. TypeScript compilation passes clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Frontend types validate data shapes before API calls |
| Component props | Props drilling passes typed data to extracted components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | Frontend `any` type bypassing compile-time checks | mitigate | Replace all `any` with proper interfaces; TypeScript strict mode enforces type checking |
| T-02-02 | I (Information Disclosure) | `catch (error: any)` hiding error types | mitigate | Use `catch (error: unknown)` + `instanceof Error` narrowing for proper error handling |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes with no errors
- `grep -rn ": any" frontend/src/` returns 0 results
- ESLint with `@typescript-eslint/no-explicit-any` passes clean
</verification>

<success_criteria>
- Frontend has zero `any` type usages across all .ts and .tsx files
- Type definition files created: User.ts, CartItem.ts, api.ts
- apiClient.ts functions have properly typed parameters
- All catch blocks use `catch (error: unknown)` with instanceof Error narrowing
- TypeScript compilation passes without errors for frontend
</success_criteria>

<output>
After completion, create `.planning/phases/02-type-safety-code-quality/02-01-SUMMARY.md`
</output>
