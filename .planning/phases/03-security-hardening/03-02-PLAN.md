---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/routes/authRoutes.ts]
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "Registration endpoint rejects passwords under 8 characters"
    - "Registration endpoint rejects passwords without uppercase letters"
    - "Registration endpoint rejects passwords without numbers"
    - "Password change and password reset endpoints do NOT enforce these rules (per D-01)"
  artifacts:
    - path: "backend/src/routes/authRoutes.ts"
      provides: "Registration and login route validation"
      min_lines: 66
      contains: "registerValidation"
  key_links:
    - from: "backend/src/routes/authRoutes.ts"
      to: "/api/auth/register"
      via: "router.post('/register')"
      pattern: "registerValidation"
    - from: "registerValidation"
      to: "body('password')"
      via: "express-validator chain"
      pattern: "isLength.*min: 8.*matches.*[A-Z].*matches.*[0-9]"
---

<objective>
Add password strength validation to the registration endpoint in backend/src/routes/authRoutes.ts. Requirements: minimum 8 characters, at least one uppercase letter, at least one number. Special characters are NOT required (per D-02). These rules apply at registration ONLY - not at password change or password reset (per D-01).
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/routes/authRoutes.ts
@.planning/phases/03-security-hardening/03-CONTEXT.md (D-01, D-02, D-03 locked decisions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing password validation rules are correct</name>
  <files>backend/src/routes/authRoutes.ts</files>
  <read_first>backend/src/routes/authRoutes.ts</read_first>
  <action>
    Read the current registerValidation in authRoutes.ts (lines 8-45). The existing validation chain already has:
      body('password').isLength({ min: 8 }).withMessage('Password must be at least 8 characters long')
      .matches(/[A-Z]/).withMessage('Password must contain at least one uppercase letter')
      .matches(/[0-9]/).withMessage('Password must contain at least one number')

    These exact rules are already in place. This task is to verify they match the locked decisions:
    - D-02: "minimum 8 characters, at least one uppercase letter, at least one number" -- CONFIRMED ALREADY CORRECT
    - D-01: Rules apply at registration only -- CONFIRMED (registerValidation is only used on /register route)
    - D-03: Special chars not enforced -- CONFIRMED (no special char validator present)

    No changes needed to the validation chain itself. The existing implementation already satisfies SEC-02.
  </action>
  <verify>
    <automated>grep -c "isLength.*min: 8" backend/src/routes/authRoutes.ts</automated>
    <automated>grep -c "matches.*\\[A-Z\\]" backend/src/routes/authRoutes.ts</automated>
    <automated>grep -c "matches.*\\[0-9\\]" backend/src/routes/authRoutes.ts</automated>
    <automated>grep -q "matches.*\\^.*\\$" backend/src/routes/authRoutes.ts && echo "FAIL: special char regex found" || echo "PASS: no special char enforcement"</automated>
  </verify>
  <done>registerValidation in authRoutes.ts correctly enforces: min 8 chars, uppercase required, number required. No special character requirement. Rules only apply to registration, not password change/reset.</done>
</task>

</tasks>

<verification>
- `grep -c "isLength.*min: 8" backend/src/routes/authRoutes.ts` returns exactly 1 (the register validation)
- `grep -c "matches.*\\[A-Z\\]" backend/src/routes/authRoutes.ts` returns exactly 1
- `grep -c "matches.*\\[0-9\\]" backend/src/routes/authRoutes.ts` returns exactly 1
- No special character regex (.matches(/[^a-zA-Z0-9]/) or similar) exists in the password validation
</verification>

<success_criteria>
Password validation rules for registration are correctly implemented per D-01, D-02, D-03. No changes needed -- existing code already satisfies SEC-02 requirements.
</success_criteria>

<output>
After completion, create .planning/phases/03-security-hardening/03-02-SUMMARY.md
</output>