---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/utils/jwt.ts]
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Server fails to start if JWT_SECRET or REFRESH_TOKEN_SECRET is missing (no silent fallback)"
    - "JWT utility module throws an informative error at load time if secrets are not configured"
  artifacts:
    - path: "backend/src/utils/jwt.ts"
      provides: "JWT token generation and verification"
      min_lines: 24
      contains: "throw new Error"
  key_links:
    - from: "backend/src/utils/jwt.ts"
      to: "process.env.JWT_SECRET"
      via: "top-level guard"
      pattern: "throw.*JWT_SECRET"
    - from: "backend/src/utils/jwt.ts"
      to: "process.env.REFRESH_TOKEN_SECRET"
      via: "top-level guard"
      pattern: "throw.*REFRESH_TOKEN_SECRET"
---

<objective>
[BLOCKING] Remove JWT secret fallback defaults in backend/src/utils/jwt.ts so the server fails immediately on startup if JWT_SECRET or REFRESH_TOKEN_SECRET is missing from the environment. No default values, no silent degradation. This prevents the server from running with weak known secrets.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/utils/jwt.ts
@.planning/phases/03-security-hardening/03-CONTEXT.md (D-01 through D-03, D-07 locked decisions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove JWT secret fallbacks with fail-fast guard</name>
  <files>backend/src/utils/jwt.ts</files>
  <read_first>backend/src/utils/jwt.ts</read_first>
  <action>
    Edit `backend/src/utils/jwt.ts` to replace the fallback defaults on lines 6-7.

    Current code (REMOVE these lines):
      const ACCESS_TOKEN_SECRET = process.env.JWT_SECRET || 'access_secret_fallback';
      const REFRESH_TOKEN_SECRET = process.env.REFRESH_TOKEN_SECRET || 'refresh_secret_fallback';

    Replace with top-level guards that throw at module load time if the env vars are missing. Use this exact pattern:

      const ACCESS_TOKEN_SECRET = process.env.JWT_SECRET;
      if (!ACCESS_TOKEN_SECRET) {
        throw new Error('[jwt] JWT_SECRET environment variable is required but not set. Server cannot start.');
      }

      const REFRESH_TOKEN_SECRET = process.env.REFRESH_TOKEN_SECRET;
      if (!REFRESH_TOKEN_SECRET) {
        throw new Error('[jwt] REFRESH_TOKEN_SECRET environment variable is required but not set. Server cannot start.');
      }

    Keep all remaining code (generateAccessToken, generateRefreshToken, verifyAccessToken, verifyRefreshToken) unchanged.
  </action>
  <verify>
    <automated>grep -c "throw new Error" backend/src/utils/jwt.ts</automated>
    <automated>grep -q "JWT_SECRET.*required" backend/src/utils/jwt.ts && grep -q "REFRESH_TOKEN_SECRET.*required" backend/src/utils/jwt.ts</automated>
    <automated>grep -q "access_secret_fallback" backend/src/utils/jwt.ts && echo "FAIL: fallback still present" || echo "PASS: no fallback found"</automated>
  </verify>
  <done>backend/src/utils/jwt.ts throws an Error at module load if JWT_SECRET or REFRESH_TOKEN_SECRET env vars are not set. No fallback strings remain. The server will crash on startup rather than running with weak secrets.</done>
</task>

</tasks>

<verification>
- `grep "throw new Error" backend/src/utils/jwt.ts` returns 2 matches
- `grep "access_secret_fallback\|refresh_secret_fallback" backend/src/utils/jwt.ts` returns no matches
- Server startup without JWT_SECRET env var causes an uncaught exception with message containing "JWT_SECRET"
</verification>

<success_criteria>
JWT utility module throws at load time if env vars missing. No fallback secrets in codebase. SEC-01 complete.
</success_criteria>

<output>
After completion, create .planning/phases/03-security-hardening/03-01-SUMMARY.md
</output>