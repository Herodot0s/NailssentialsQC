---
phase: "03"
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/controllers/authController.ts]
autonomous: true
requirements: [SEC-06]
must_haves:
  truths:
    - "Refresh token rotation creates new token BEFORE deleting the old one (no race condition gap)"
    - "If server crashes between create and delete, the old token can still be used to re-authenticate"
  artifacts:
    - path: "backend/src/controllers/authController.ts"
      provides: "Refresh token rotation with correct ordering"
      contains: "refreshToken.create"
      order_matters: true
  key_links:
    - from: "authController refresh"
      to: "prisma.refreshToken.create"
      via: "token rotation create-first ordering"
      pattern: "refreshToken\\.create.*before.*refreshToken\\.delete"
    - from: "authController refresh"
      to: "prisma.refreshToken.delete"
      via: "token rotation delete-after ordering"
      pattern: "refreshToken\\.delete.*after.*refreshToken\\.create"
---

<objective>
Fix the refresh token rotation ordering in authController.ts refresh function. The current code deletes the old token BEFORE creating the new one, creating a race condition gap. Per SEC-06 decision: create the new refresh token BEFORE deleting the old one. Ordering alone is sufficient (no explicit transaction needed).
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/controllers/authController.ts (lines 299-325 for refresh token rotation)
@.planning/phases/03-security-hardening/03-CONTEXT.md (SEC-06 decision: create new token before deleting old, ordering alone is sufficient)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix refresh token rotation ordering — create before delete</name>
  <files>backend/src/controllers/authController.ts</files>
  <read_first>backend/src/controllers/authController.ts (lines 299-325, full refresh function)</read_first>
  <action>
    In `backend/src/controllers/authController.ts`, locate the `refresh` function and the refresh token rotation block (around lines 311-324).

    Current code (INCORRECT - delete before create):
      // Delete old, store new
      await prisma.refreshToken.delete({
        where: { id: storedToken.id },
      });

      const expiresAt = new Date();
      expiresAt.setDate(expiresAt.getDate() + 30);
      await prisma.refreshToken.create({
        data: {
          user_id: storedToken.user.id,
          token: newRefreshToken,
          expires_at: expiresAt,
        },
      });

    FIX: Swap the order so create happens BEFORE delete:

      // SEC-06: Create new token first, then delete old
      // This ensures there is always a valid token (no race condition gap)
      const expiresAt = new Date();
      expiresAt.setDate(expiresAt.getDate() + 30);
      await prisma.refreshToken.create({
        data: {
          user_id: storedToken.user.id,
          token: newRefreshToken,
          expires_at: expiresAt,
        },
      });

      await prisma.refreshToken.delete({
        where: { id: storedToken.id },
      });

    The `expiresAt` calculation should remain with the create (not duplicated). The `newRefreshToken` variable (generated at lines 307-309) must already exist before this block — it does, so no changes needed to the token generation lines.

    Do NOT add a Prisma transaction. Per SEC-06: "ordering alone is sufficient" — no explicit $transaction wrapper needed. Swapping the order of the two existing async calls is the complete fix.
  </action>
  <verify>
    <automated>grep -n "refreshToken.create" backend/src/controllers/authController.ts | grep -v grep</automated>
    <automated>grep -n "refreshToken.delete" backend/src/controllers/authController.ts | grep -v grep</automated>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('backend/src/controllers/authController.ts', 'utf8'); const createPos = content.indexOf('refreshToken.create'); const deletePos = content.indexOf('refreshToken.delete'); if (createPos < deletePos) { console.log('PASS: create appears before delete in file'); } else { console.log('FAIL: delete appears before create in file'); }"</automated>
  </verify>
  <done>authController.ts refresh function creates new refreshToken BEFORE deleting the old one. The create block (with expiresAt calculation) now precedes the delete call. No transaction wrapper added — ordering change is the complete fix per SEC-06 decision.</done>
</task>

</tasks>

<verification>
- `grep -n "refreshToken.create" backend/src/controllers/authController.ts` shows the create call exists and appears before the delete call
- `grep -n "refreshToken.delete" backend/src/controllers/authController.ts` shows the delete call exists and appears after the create call
- Node.js check confirms create appears at a lower character position than delete in the file
</verification>

<success_criteria>
Refresh token rotation creates new token before deleting old. No race condition gap exists. If server crashes between create and delete, user can still authenticate with old token. SEC-06 complete.
</success_criteria>

<output>
After completion, create .planning/phases/03-security-hardening/03-06-SUMMARY.md
</output>