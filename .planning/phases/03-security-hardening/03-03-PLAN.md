---
phase: "03"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/controllers/appointmentController.ts, backend/src/controllers/attendanceController.ts]
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - "Customers cannot create notifications for other users (cross-user notifications blocked with WARNING log)"
    - "Role check enforced at each caller of createNotification(), not inside createNotification() itself"
    - "Staff and manager roles have no cross-role restrictions on notification creation"
  artifacts:
    - path: "backend/src/controllers/appointmentController.ts"
      provides: "Role guard at createNotification call in createAppointment"
    - path: "backend/src/controllers/attendanceController.ts"
      provides: "Role guard at createNotification calls in checkIn and checkOut"
  key_links:
    - from: "appointmentController.ts createAppointment"
      to: "createNotification"
      via: "fire-and-forget async block"
      pattern: "createNotification.*staff.user_id"
    - from: "attendanceController.ts checkIn/checkOut"
      to: "createNotification"
      via: "fire-and-forget async block"
      pattern: "createNotification.*manager.id"
---

<objective>
Add role-based permission checks at each caller of createNotification() per D-04 and D-05. Guards check if the caller is a customer attempting to notify a different user (cross-user notification). If blocked, log WARNING per D-07 and skip the notification silently (no 403 response).
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/controllers/appointmentController.ts
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/controllers/attendanceController.ts
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/controllers/notificationController.ts (createNotification signature)
@.planning/phases/03-security-hardening/03-CONTEXT.md (D-04, D-05, D-07 locked decisions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role guard at createNotification call in appointmentController.ts</name>
  <files>backend/src/controllers/appointmentController.ts</files>
  <read_first>backend/src/controllers/appointmentController.ts (lines 1-20, 53-65, 140-175)</read_first>
  <action>
    In `backend/src/controllers/appointmentController.ts`, the `createAppointment` function uses a fire-and-forget async block after line 140 to send notifications to assigned staff. The issue: the async block has no access to req.user.

    SOLUTION: Capture callerId and callerRole BEFORE the async block starts (before line 140 in createAppointment). Add a guard when calling createNotification.

    In createAppointment, after line 56 (`const { role } = req.user || {};`), add:
      const callerId = userId;
      const callerRole = role;

    Then at the createNotification call inside the async block (lines 157-167), add a guard before the call:
      const targetUserId = staff.user_id;
      if (callerRole === 'customer' && callerId !== targetUserId) {
        // D-07: log warning, skip silently (no 403)
        console.warn('[notification] Blocked cross-user notification attempt', { callerId, targetId: targetUserId });
      } else {
        createNotification(targetUserId, 'NEW_BOOKING', 'New Appointment Assigned', `You have a new booking on ${format(new Date(date), 'MMM dd')} at ${item.startTime}.`);
      }

    Note: The `callerRole` and `callerId` variables must be defined INSIDE the outer async () => { ... } block BEFORE the inner async notification block, so they are accessible via closure from the outer function's scope. Actually, since the notification block is already `async () => { ... }` running inside createAppointment, the simplest fix is to move the role/callerId capture to the outer scope (inside createAppointment) before the notification async block.

    The current code (lines 140-171):
      (async () => {
        try {
          ...
          for (const item of items) {
            const staff = await prisma.staffProfile.findUnique(...);
            if (staff) {
              createNotification(staff.user_id, 'NEW_BOOKING', ...);
            }
          }
        }
      })();

    Change to:
      const callerId = userId;   // already available from line 55
      const callerRole = role;  // already available from line 56

      (async () => {
        try {
          ...
          for (const item of items) {
            const staff = await prisma.staffProfile.findUnique(...);
            if (staff) {
              // D-05 guard: customers cannot notify other users
              if (callerRole === 'customer' && callerId !== staff.user_id) {
                console.warn('[notification] Blocked cross-user notification attempt', { callerId, targetId: staff.user_id });
              } else {
                createNotification(staff.user_id, 'NEW_BOOKING', ...);
              }
            }
          }
        }
      })();
  </action>
  <verify>
    <automated>grep -c "callerRole.*customer" backend/src/controllers/appointmentController.ts</automated>
    <automated>grep -c "Blocked cross-user notification attempt" backend/src/controllers/appointmentController.ts</automated>
    <automated>grep -q "console.warn.*notification.*callerId" backend/src/controllers/appointmentController.ts && echo "PASS: D-07 logging pattern found" || echo "FAIL: D-07 logging pattern missing"</automated>
  </verify>
  <done>appointmentController.ts createAppointment has callerRole/callerId captured before the async notification block. Guard: if callerRole is 'customer' and callerId != targetUserId, log warning and skip createNotification call. No 403 response returned.</done>
</task>

<task type="auto">
  <name>Task 2: Add role guards at createNotification calls in attendanceController.ts</name>
  <files>backend/src/controllers/attendanceController.ts</files>
  <read_first>backend/src/controllers/attendanceController.ts (lines 80-195 for checkIn, 198-250 for checkOut)</read_first>
  <action>
    In `backend/src/controllers/attendanceController.ts`, two functions call createNotification:
    1. `checkIn` (around line 167-177): notifies managers when staff checks in
    2. `checkOut` (around line 232-243): notifies managers when staff checks out

    In `checkIn`, find where the async notification block starts. Before that async block, capture callerId and callerRole. Then inside the loop over managers, add the guard.

    For checkIn: The notification block starts at line 166 (`// Notify Managers (async () => {`). Before line 166, add:
      const callerId = userId;
      const callerRole = req.user?.role;

    Then in the for loop (lines 170-177), change:
      createNotification(manager.id, 'STAFF_CHECK_IN', ...);

    To:
      if (callerRole === 'customer' && callerId !== manager.id) {
        console.warn('[notification] Blocked cross-user notification attempt', { callerId, targetId: manager.id });
      } else {
        createNotification(manager.id, 'STAFF_CHECK_IN', ...);
      }

    For checkOut: The notification block starts at line 231 (`// Notify Managers (async () => {` or same pattern). Before that async block, add the same capture:
      const callerId = userId;
      const callerRole = req.user?.role;

    Then in the for loop over managers, add the identical guard before createNotification.
  </action>
  <verify>
    <automated>grep -c "callerRole.*customer" backend/src/controllers/attendanceController.ts</automated>
    <automated>grep -c "Blocked cross-user notification attempt" backend/src/controllers/attendanceController.ts</automated>
    <automated>grep -q "console.warn.*notification.*callerId" backend/src/controllers/attendanceController.ts && echo "PASS: D-07 pattern found" || echo "FAIL: D-07 pattern missing"</automated>
  </verify>
  <done>Both checkIn and checkOut in attendanceController.ts have callerRole/callerId captured before their async notification blocks. Guard: if callerRole is 'customer' and callerId != targetManagerId, log warning and skip the notification. No 403 response returned.</done>
</task>

</tasks>

<verification>
- `grep -c "callerRole.*customer" backend/src/controllers/appointmentController.ts` returns exactly 1
- `grep -c "callerRole.*customer" backend/src/controllers/attendanceController.ts` returns exactly 2 (one per function)
- `grep -c "Blocked cross-user notification attempt" backend/src/controllers/appointmentController.ts` returns exactly 1
- `grep -c "Blocked cross-user notification attempt" backend/src/controllers/attendanceController.ts` returns exactly 2
- Both occurrences use the D-07 logging pattern: `console.warn('[notification] Blocked cross-user notification attempt', { callerId, targetId: ... })`
</verification>

<success_criteria>
Role checks are enforced at each caller of createNotification. Customers are blocked from cross-user notifications with WARNING-level log. Staff/manager notification paths are unaffected. SEC-03 complete.
</success_criteria>

<output>
After completion, create .planning/phases/03-security-hardening/03-03-SUMMARY.md
</output>