---
phase: 02-customer-journey-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/App.tsx",
  "frontend/src/App.test.tsx",
  "frontend/src/pages/Home.tsx",
  "frontend/src/pages/Home.test.tsx",
  "frontend/src/components/home/Hero.tsx",
  "frontend/src/components/home/Hero.test.tsx",
  "frontend/src/components/home/TrendingTreatments.tsx",
  "frontend/src/components/home/TrendingTreatments.test.tsx"
]
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Test infrastructure exists for all new landing page components"
    - "Landing page is a dedicated component at /pages/Home.tsx"
    - "Hero section uses photography-led design with premium typography (22px-28px weight 500)"
    - "Landing page features a 'Trending Treatments' section driven by is_popular flag"
    - "Cards and main containers use rounded-3xl (32px) radius"
    - "Primary CTAs use Rausch (#FF385C) color"
  artifacts:
    - path: "frontend/src/pages/Home.test.tsx"
      provides: "Unit test for the Home page"
    - path: "frontend/src/components/home/Hero.test.tsx"
      provides: "Unit test for the Hero section"
    - path: "frontend/src/components/home/TrendingTreatments.test.tsx"
      provides: "Unit test for the Trending Treatments section"
    - path: "frontend/src/pages/Home.tsx"
      provides: "New dedicated Landing Page component"
    - path: "frontend/src/components/home/TrendingTreatments.tsx"
      provides: "Dynamic grid for popular services"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Home.tsx"
      via: "React Router Route"
    - from: "frontend/src/components/home/TrendingTreatments.tsx"
      to: "apiClient.getServices"
      via: "API call for popular services"
---

<objective>
Refactor the landing page into a dedicated component and implement the premium photography-first entry experience with "Trending Treatments", supported by a verified test suite.

Purpose: Establish the boutique, high-end "Sanctuary" brand identity from the first user touchpoint while ensuring visual regression safety.
Output: Test suite, Dedicated Home page, Photography Hero, and Dynamic Trending grid.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-customer-journey-redesign/02-CONTEXT.md
@.planning/phases/02-customer-journey-redesign/RESEARCH.md
@airbnb/DESIGN.md
@frontend/src/App.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 0: Test Scaffolding (Wave 0)</name>
  <files>frontend/src/App.test.tsx, frontend/src/pages/Home.test.tsx, frontend/src/components/home/Hero.test.tsx, frontend/src/components/home/TrendingTreatments.test.tsx</files>
  <action>
    - Create basic smoke tests for all core components involved in this plan.
    - `App.test.tsx`: Verify the application renders without crashing.
    - `Home.test.tsx`: Verify the Home page component renders.
    - `Hero.test.tsx`: Verify the Hero component renders.
    - `TrendingTreatments.test.tsx`: Verify the TrendingTreatments component renders and handles a mock list of popular services.
    - Use Vitest and React Testing Library patterns. Mock any external assets (Unsplash images) or heavy font providers if they interfere with test execution.
  </action>
  <verify>
    <automated>npx vitest run frontend/src/App.test.tsx frontend/src/pages/Home.test.tsx frontend/src/components/home/Hero.test.tsx frontend/src/components/home/TrendingTreatments.test.tsx</automated>
  </verify>
  <done>Basic test infrastructure is in place and passing for all planned artifacts.</done>
</task>

<task type="auto">
  <name>Task 1: Landing Page Refactor & Routing</name>
  <files>frontend/src/pages/Home.tsx, frontend/src/App.tsx</files>
  <action>
    - Create `frontend/src/pages/Home.tsx` and move the current `Home` component logic from `App.tsx` into it (per D-01).
    - Update `frontend/src/App.tsx` to import `Home` from `./pages/Home` and use it within the root `/` route.
    - Ensure `Navbar` and `Footer` (if any) are correctly integrated with the new page structure.
    - Update `App.test.tsx` and `Home.test.tsx` to reflect the new structure.
  </action>
  <verify>
    <automated>npx vitest run frontend/src/App.test.tsx frontend/src/pages/Home.test.tsx</automated>
  </verify>
  <done>Landing page is successfully isolated in its own file and accessible at the root URL.</done>
</task>

<task type="auto">
  <name>Task 2: Photography-Led Hero Section</name>
  <files>frontend/src/components/home/Hero.tsx, frontend/src/pages/Home.tsx</files>
  <action>
    - Extract Hero section into `frontend/src/components/home/Hero.tsx`.
    - Implement a photography-first design per D-02:
      - Use high-quality Unsplash imagery (e.g., salon interiors or close-up nail artistry).
      - Use premium typography: Titles 22px-28px, weight 500, font-serif (Playfair Display).
      - Apply `rounded-3xl` (32px) to any overlay containers or featured imagery blocks (per D-07).
      - Update Primary CTA ("Book Your Sanctuary") to use Rausch (#FF385C) background (per D-08).
  </action>
  <verify>
    <automated>npx vitest run frontend/src/components/home/Hero.test.tsx --testNamePattern="Hero renders with Rausch CTA"</automated>
  </verify>
  <done>Hero section reflects the premium "Sanctuary" aesthetic with correct tokens and photography focus, verified by automated class checks.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic 'Trending Treatments' Section</name>
  <files>frontend/src/components/home/TrendingTreatments.tsx, frontend/src/pages/Home.tsx</files>
  <action>
    - Create `frontend/src/components/home/TrendingTreatments.tsx`.
    - Fetch all services via `apiClient.getServices()`.
    - Filter services to show only those where `is_popular === true` (per D-03).
    - Render in a clean grid layout using the "Inspiration for future getaways" pattern (Airbnb style).
    - Cards MUST use `rounded-3xl` (32px) for the image container and `rounded-xl` (12px) for buttons.
    - Each card should include an "Explore" button that links to `/services` or opens the new Modal (Modal implemented in next plan).
  </action>
  <verify>
    <automated>npx vitest run frontend/src/components/home/TrendingTreatments.test.tsx</automated>
  </verify>
  <done>Trending Treatments section dynamically displays popular services with premium card styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted service data is fetched and rendered on the Home page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Home.tsx | mitigate | Sanitize any dynamic text from the API before rendering to prevent XSS. |
| T-02-01-02 | Denial of Service | TrendingTreatments.tsx | mitigate | Limit the number of popular services displayed (e.g., top 6) to prevent excessive DOM nodes if the DB flag is overused. |
</threat_model>

<verification>
- Visit `/` and verify the new photography-led Hero section appears.
- Verify "Trending Treatments" section loads and displays popular services (mock or seed data).
- Check responsive layout on mobile (specifically the grid and hero text scaling).
- Confirm Rausch (#FF385C) is used for the main button.
</verification>

<success_criteria>
- Landing page refactored into `pages/Home.tsx`.
- Hero section updated with premium design tokens and typography.
- "Trending Treatments" grid implemented and showing dynamic data.
- UI elements follow the `rounded-3xl` and Rausch token rules.
- 100% pass rate for the new landing page test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-journey-redesign/02-01-SUMMARY.md`
</output>
