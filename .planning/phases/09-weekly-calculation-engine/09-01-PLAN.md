---
phase: 09-weekly-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/prisma/schema.prisma", "backend/src/controllers/payrollController.ts"]
autonomous: true
requirements: ["PAY-03"]

must_haves:
  truths:
    - "Schema contains daily_breakdown JSON field for StaffPayroll"
    - "Payroll calculation matches spreadsheet behavior with daily aggregation"
    - "Manual deductions are preserved during period regeneration"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Updated database schema"
      contains: "daily_breakdown"
    - path: "backend/src/controllers/payrollController.ts"
      provides: "Accurate weekly payroll generation and regeneration logic"
  key_links: []
---

<objective>
Update database schema to support JSON snapshot of daily performance, push schema, and implement the Weekly Calculation Engine in `payrollController.ts`.

Purpose: Enable accurate weekly payroll generation mirroring spreadsheet behaviors.
Output: Updated schema.prisma, synced DB, and functional payroll generation endpoint.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-weekly-calculation-engine/09-CONTEXT.md
@.planning/phases/09-weekly-calculation-engine/09-RESEARCH.md
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files. The planner MUST include
a `[BLOCKING]` task that runs the database schema push command AFTER all schema file
modifications are complete but BEFORE verification.

- ORM detected: Prisma
- Push command: npx prisma db push
- Non-TTY workaround: npx prisma db push --accept-data-loss
- If push requires interactive prompts that cannot be suppressed, flag the task for
  manual intervention with `autonomous: false`

This task is mandatory — the phase CANNOT pass verification without it. Build and
type checks will pass without the push (types come from config, not the live database),
creating a false-positive verification state.
</schema_push_requirement>

<tasks>

<task type="auto">
  <name>Task 1: Update schema.prisma for StaffPayroll</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>
    1. In `StaffPayroll` model, add `daily_breakdown Json?` below the `status` field.
  </action>
  <verify>cd backend && npx prisma validate</verify>
  <acceptance_criteria>
    - `StaffPayroll` has `daily_breakdown Json?` field.
  </acceptance_criteria>
  <done>Prisma schema validation passes</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Schema Push Required</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>Run the database push command: `cd backend && npx prisma db push --accept-data-loss` to sync the new model with the live database.</action>
  <verify>Check the output for success message</verify>
  <acceptance_criteria>
    - `npx prisma db push` executes successfully
    - Database is fully synced with the new schema
  </acceptance_criteria>
  <done>Database schema updated</done>
</task>

<task type="auto">
  <name>Task 3: Implement Calculation Engine in generatePayroll</name>
  <files>backend/src/controllers/payrollController.ts</files>
  <read_first>backend/src/controllers/payrollController.ts</read_first>
  <action>
    1. Update `generatePayroll` endpoint.
    2. Read the `base_commission_rate` and `commission_tier` from `StaffProfile`. If `commission_tier` exists but `base_commission_rate` is missing or 0, default to 8% (0.08) for tier calculations if needed (though `base_commission_rate` is the source of truth).
    3. Aggregate daily performance from the `Commission` records for the period dates. Create a JSON structure mapping dates to total sales. Store this in `daily_breakdown`.
    4. Implement Wipe and Regenerate logic: If `StaffPayroll` records already exist for the period, delete them before regenerating. However, for `DeductionLog` records associated with this period, detach them or preserve them so they are not deleted, ensuring manual deductions (Cash Advance, Uniform, etc.) persist across regenerations.
  </action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `generatePayroll` properly calculates and stores `daily_breakdown` JSON.
    - Commission rate is pulled from staff profile and applied.
    - Wiping and regenerating preserves existing `DeductionLog` entries.
  </acceptance_criteria>
  <done>TypeScript build succeeds and logic handles calculation</done>
</task>

<task type="auto">
  <name>Task 4: Add Validation Script</name>
  <files>backend/src/scripts/testPayrollAPI.ts</files>
  <read_first></read_first>
  <action>Create a simple test script `backend/src/scripts/testPayrollAPI.ts` to exercise the generate logic to ensure it doesn't crash.</action>
  <verify>cd backend && npx ts-node src/scripts/testPayrollAPI.ts (or just rely on typescript compilation)</verify>
  <acceptance_criteria>
    - Script exists and compiles
  </acceptance_criteria>
  <done>Script created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma db push` was successfully run
- [ ] Logic for daily aggregation is verified against requirements
</verification>

<success_criteria>
- Schema correctly represents daily_breakdown.
- Database is updated without errors
- Backend payroll engine handles daily JSON aggregation, commission logic, and safe regeneration.
</success_criteria>

<output>
After completion, create `.planning/phases/09-weekly-calculation-engine/09-01-SUMMARY.md`
</output>
