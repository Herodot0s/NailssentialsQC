---
phase: 10-manager-payroll-ui-excel-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/controllers/payrollController.ts, backend/src/routes/payrollRoutes.ts]
autonomous: true
requirements: [PAY-06, PAY-01]

must_haves:
  truths:
    - "Excel export has Staff as rows and Dates as columns"
    - "Excel export includes categorized deductions as separate columns"
    - "Excel export includes a [DRAFT] watermark or header for non-locked periods"
  artifacts:
    - path: "backend/src/controllers/payrollController.ts"
      provides: "Transposed Excel export logic"
      contains: "Staff are Rows"
---

<objective>
Refactor the Excel export logic to use the transposed layout (Staff as Rows, Dates as Columns) and include all categorized metrics.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-manager-payroll-ui-excel-export/10-CONTEXT.md
@.planning/phases/10-manager-payroll-ui-excel-export/10-RESEARCH.md
@backend/src/controllers/payrollController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor exportPayrollExcel for Transposition</name>
  <files>backend/src/controllers/payrollController.ts</files>
  <read_first>backend/src/controllers/payrollController.ts</read_first>
  <action>
    Flip the Excel layout:
    1. Header row: ["Staff Name", ...dates, "Total Sales", "Commission Pay", "Basic Pay", "Gross Pay", "CA", "Loan", "Uniform", "Reloan", "Lates/Early Out", "Total Deductions", "Net Pay"].
    2. One row per staff member.
    3. For each staff row, populate date columns from daily_breakdown.
    4. Populate summary columns from StaffPayroll and DeductionLog.
    5. Add "DRAFT - NOT FINALIZED" header if period.is_locked is false.
    6. Prefix filename with [DRAFT] if not locked.
  </action>
  <verify>npm test backend/src/tests/payroll.test.ts (after creating test in Plan 02 or Wave 0)</verify>
  <done>ExcelJS logic produces transposed rows for each staff member.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure API Routes support locking and deduction management</name>
  <files>backend/src/routes/payrollRoutes.ts</files>
  <read_first>backend/src/routes/payrollRoutes.ts, backend/src/controllers/payrollController.ts</read_first>
  <action>
    Verify/Add routes for:
    - POST /api/payroll/deductions (addDeduction)
    - DELETE /api/payroll/deductions/:id (deleteDeduction)
    - POST /api/payroll/periods/:id/lock (lockPayroll)
    - GET /api/payroll/periods (getPayrollPeriods)
    Ensure all are protected by authorizeRoles('manager').
  </action>
  <verify>Check routes/payrollRoutes.ts for exports matching controller functions.</verify>
  <done>All backend endpoints required for the Manager UI are exposed and protected.</done>
</task>

</tasks>

<verification>
- [ ] Excel export contains staff names in the first column.
- [ ] Excel export contains dates in the header row.
- [ ] Backend routes are correctly mapped.
</verification>

<success_criteria>
- Excel transposition is correct.
- Categorized deductions are present in the export.
- Backend API is ready for frontend consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/10-manager-payroll-ui-excel-export/10-01-SUMMARY.md`
</output>
