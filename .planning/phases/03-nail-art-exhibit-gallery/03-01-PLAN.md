---
phase: 03-nail-art-exhibit-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/prisma/schema.prisma, backend/src/controllers/exhibitController.ts, backend/src/routes/exhibitRoutes.ts, backend/src/index.ts]
autonomous: true
requirements: [CMS-01, CMS-02]

must_haves:
  truths:
    - "Exhibit model exists in the database with relations to StaffProfile and Service"
    - "API endpoints for CRUD operations on exhibits are functional and secured"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Exhibit model definition"
    - path: "backend/src/controllers/exhibitController.ts"
      provides: "CRUD logic for exhibits"
    - path: "backend/src/routes/exhibitRoutes.ts"
      provides: "API route definitions"
  key_links:
    - from: "backend/src/routes/exhibitRoutes.ts"
      to: "backend/src/controllers/exhibitController.ts"
      via: "Route mapping"
    - from: "backend/src/index.ts"
      to: "backend/src/routes/exhibitRoutes.ts"
      via: "App registration"
---

<objective>
Establish the data foundation for the Nail Art Exhibit. This includes updating the Prisma schema, creating the necessary API controllers and routes, and ensuring the backend can persist and serve exhibit metadata linked to artists (StaffProfile) and services.

Purpose: Provide a robust API for the gallery CMS and public view.
Output: Database migration and functional CRUD API endpoints.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/prisma/schema.prisma
@backend/src/controllers/uploadController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma schema and run migration</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
    - Add the `Exhibit` model to `backend/prisma/schema.prisma` per D-04, D-05, D-06.
    - Fields: `id` (autoincrement), `title` (String), `image_url` (String), `staff_id` (Int), `service_id` (Int, optional), `is_active` (Boolean, default true), `created_at`, `updated_at`.
    - Relations: `artist` (links to StaffProfile via `staff_id`), `service` (links to Service via `service_id`, optional).
    - Add back-relations to `StaffProfile` (`exhibits Exhibit[]`) and `Service` (`exhibits Exhibit[]`).
    - Run `npx prisma migrate dev --name add_exhibit_model` in the backend directory.
  </action>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>Prisma schema is updated and migration is applied to the database.</done>
</task>

<task type="auto">
  <name>Task 2: Create Exhibit Controller and Routes</name>
  <files>backend/src/controllers/exhibitController.ts, backend/src/routes/exhibitRoutes.ts</files>
  <action>
    - Create `backend/src/controllers/exhibitController.ts` with the following functions:
      - `getAllExhibits`: Fetch all active exhibits, including artist and service info.
      - `createExhibit`: Protected (Manager), accepts metadata and creates record.
      - `deleteExhibit`: Protected (Manager), deletes record and should also handle Blob deletion (using logic from uploadController.ts if applicable, per D-08).
    - Create `backend/src/routes/exhibitRoutes.ts` to map these functions to endpoints.
    - Ensure `createExhibit` and `deleteExhibit` use the `protect` and `restrictTo('manager')` middlewares (check authRoutes.ts for pattern).
  </action>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <done>Controller and Routes are created with proper authorization checks.</done>
</task>

<task type="auto">
  <name>Task 3: Register Exhibit routes in App entry point</name>
  <files>backend/src/index.ts</files>
  <action>
    - Import `exhibitRoutes` in `backend/src/index.ts`.
    - Register it under `app.use('/api/v1/exhibits', exhibitRoutes)`.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/v1/exhibits | grep -q "success"</automated>
  </verify>
  <done>API is accessible at /api/v1/exhibits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Exhibit API | External requests to manage gallery content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Elevation of Privilege | /api/v1/exhibits (POST/DELETE) | mitigate | Apply RBAC middleware to restrict write operations to 'manager' role only. |
| T-03-01-02 | Information Disclosure | /api/v1/exhibits (GET) | mitigate | Ensure only necessary fields are returned (e.g., don't leak staff phone numbers if included in relation). |
</threat_model>

<verification>
1. Run Prisma migration and verify `Exhibit` table exists.
2. Verify API endpoints return success responses using `curl` or Postman.
3. Check that non-managers receive 403 on POST/DELETE.
</verification>

<success_criteria>
- Exhibit table created in database.
- GET `/api/v1/exhibits` returns an empty array (or items if populated).
- POST `/api/v1/exhibits` returns 401/403 for unauthorized users.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nail-art-exhibit-gallery/03-01-SUMMARY.md`
</output>
