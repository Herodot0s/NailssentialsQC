---
phase: 03-nail-art-exhibit-gallery
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [frontend/src/pages/ManageExhibits.tsx, frontend/src/components/gallery/ExhibitForm.tsx, frontend/src/App.tsx, frontend/src/pages/ManagerDashboard.tsx]
autonomous: true
requirements: [CMS-02]

must_haves:
  truths:
    - "Managers can upload new nail art images with metadata"
    - "The CMS displays a list of existing exhibit items with a delete option"
  artifacts:
    - path: "frontend/src/pages/ManageExhibits.tsx"
      provides: "Exhibit management page"
    - path: "frontend/src/components/gallery/ExhibitForm.tsx"
      provides: "Form for creating/editing exhibits"
  key_links:
    - from: "frontend/src/components/gallery/ExhibitForm.tsx"
      to: "/api/upload"
      via: "Media upload"
    - from: "frontend/src/components/gallery/ExhibitForm.tsx"
      to: "/api/v1/exhibits"
      via: "Metadata persistence"
---

<objective>
Build the Management CMS for the Nail Art Exhibit. This allows salon managers to curate the gallery by uploading high-resolution images, assigning artist credits, and optionally linking them to services in the catalog.

Purpose: Enable salon staff to manage their public portfolio without technical assistance.
Output: A functional "Manage Exhibits" page in the manager dashboard.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-nail-art-exhibit-gallery/03-01-SUMMARY.md
@frontend/src/pages/ManageServices.tsx
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Exhibit Form Component</name>
  <files>frontend/src/components/gallery/ExhibitForm.tsx</files>
  <action>
    - Create a form component using shadcn UI components (Dialog, Form, Input, Select, Button).
    - Features:
      - Image file picker (Input type="file").
      - Title field (Input).
      - Artist selection (Select, populated from `/api/v1/staff`).
      - Service selection (Select, optional, populated from `/api/v1/services`).
    - Logic:
      - First, upload image to `/api/upload` (multipart/form-data) to get the Vercel Blob URL.
      - Then, POST metadata + URL to `/api/v1/exhibits`.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>ExhibitForm component is built with upload logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create Manage Exhibits Page</name>
  <files>frontend/src/pages/ManageExhibits.tsx</files>
  <action>
    - Create `frontend/src/pages/ManageExhibits.tsx`.
    - Features:
      - "Add New Exhibit" button that opens the `ExhibitForm` in a Dialog.
      - A list or grid of existing exhibits (fetched from `/api/v1/exhibits`).
      - Each item should have a "Delete" button that calls the DELETE API.
    - Style following existing manager pages (e.g., `ManageServices.tsx`).
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>ManageExhibits page is functional and follows design patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Register route and add dashboard link</name>
  <files>frontend/src/App.tsx, frontend/src/pages/ManagerDashboard.tsx</files>
  <action>
    - Add `/manage-exhibits` route to `frontend/src/App.tsx`, protected for `manager` role.
    - Add a link/card to `frontend/src/pages/ManagerDashboard.tsx` to navigate to the new page.
  </action>
  <verify>
    <automated>grep -q "/manage-exhibits" frontend/src/App.tsx</automated>
  </verify>
  <done>CMS is accessible via the manager dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Manager Dashboard -> Browser | Sensitive management actions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Elevation of Privilege | /manage-exhibits route | mitigate | Enforce role-based access control at the route level in App.tsx. |
| T-03-02-02 | Cross-Site Request Forgery (CSRF) | Image Upload | mitigate | Ensure the frontend sends appropriate auth headers (JWT) for all upload and API requests. |
</threat_model>

<verification>
1. Login as Manager.
2. Navigate to "Manage Exhibits".
3. Upload a test image with metadata.
4. Verify the item appears in the list.
5. Delete the test item.
</verification>

<success_criteria>
- Manager can navigate to `/manage-exhibits`.
- Manager can upload an image and see it persisted in the list.
- Manager can delete an exhibit item.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nail-art-exhibit-gallery/03-02-SUMMARY.md`
</output>
