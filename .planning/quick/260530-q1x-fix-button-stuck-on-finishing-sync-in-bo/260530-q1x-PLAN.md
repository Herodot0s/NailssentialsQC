---
phase: 260530-q1x-fix-button-stuck-on-finishing-sync-in-bo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/pages/Booking.tsx]
autonomous: true
requirements: []
---

<objective>
Fix the button state in `Booking.tsx` to prevent it from getting stuck on "FINISHING SYNC..." when `isSignedIn` is true but `isAuthenticated` is false.

Purpose: Improve user experience during the auth sync process.
Output: Responsive booking button state.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/pages/Booking.tsx
@frontend/src/context/AuthContext.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update Booking button condition</name>
  <files>frontend/src/pages/Booking.tsx</files>
  <action>
    Modify the booking button in `Booking.tsx` to handle the `isAuthenticated` check more gracefully.
    Specifically, update the condition for 'FINISHING SYNC...' to only show if `isSignedIn` is true AND `isLoading` (from AuthContext) is true, 
    instead of relying on `!isAuthenticated && !authLoading`. This ensures the UI reflects the actual loading state rather than a sync failure or incomplete sync state.
  </action>
  <verify>
    <automated>npm run lint:frontend</automated>
  </verify>
  <done>Button correctly transitions from "FINISHING SYNC..." to "SCHEDULE APPOINTMENT" when Auth sync completes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API | UI state logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure | UI State | accept | No PII in UI state |
</threat_model>

<verification>
<automated>npm run lint:frontend</automated>
</verification>

<success_criteria>
Button state logic correctly identifies auth loading state.
</success_criteria>
