---
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SliderCheckIn.tsx
autonomous: true
---

# Quick Task: remove-clock

## Task 1: Remove Clock Display from SliderCheckIn

<read_first>
- frontend/src/components/SliderCheckIn.tsx (full file — the clock display to remove and the layout to simplify)
</read_first>

<objective>
Remove the large clock display (font-serif text-4xl) and date display from the SliderCheckIn header, simplify to just status + last action.
</objective>

<action>
Edit `frontend/src/components/SliderCheckIn.tsx`:

1. Remove the clock display paragraph and the date paragraph from the left side (inside the `space-y-1` div that contains status dot, clock, and date):

   REMOVE these two elements from the first `<div className="space-y-1">`:
   ```tsx
   <p className="font-serif text-4xl font-light text-foreground">
     {formatTime(currentTime)}
   </p>
   <p className="text-[10px] uppercase tracking-[0.2em] text-muted-foreground font-bold">
     {formatDate(currentTime)}
   </p>
   ```

2. Also remove the `formatTime` and `formatDate` function definitions since they're no longer used:
   ```tsx
   const formatTime = (date: Date) =>
     date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });

   const formatDate = (date: Date) =>
     date.toLocaleDateString([], { weekday: 'long', month: 'long', day: 'numeric' });
   ```

3. Remove the `currentTime` prop from the component signature and from the destructured props, since it's no longer needed:
   - Remove `currentTime: Date;` from `SliderCheckInProps` interface
   - Remove `currentTime,` from the destructured props in the component parameter

4. Update StaffDashboard.tsx to stop passing `currentTime` prop:
   In the `<SliderCheckIn>` usage, REMOVE the line:
   ```tsx
   currentTime={currentTime}
   ```

5. Simplify the header layout — since the left side only has the status div now, remove the `justify-between` and the right-side div, and put the "Last: —" and "Today: Xh Ym" info inline with the status:

   REPLACE the entire header section (the outer `div` with `px-6 pt-6 pb-4 flex items-start justify-between`) with:

   ```tsx
   <div className="px-6 pt-6 pb-4 flex items-center justify-between">
     <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest ${
       isCheckedIn
         ? 'bg-success-color/10 text-success-color'
         : 'bg-muted/20 text-muted-foreground'
     }`}
   >
     <span
       className={`w-2 h-2 rounded-full ${
         isCheckedIn ? 'bg-success-color' : 'bg-muted-foreground'
       }`}
     />
     {isCheckedIn ? 'Active' : 'Off Duty'}
   </div>

   <div className="text-right space-y-1">
     <p className="text-[9px] uppercase tracking-widest text-muted-foreground font-bold">
       {isCheckedIn && checkInTime ? `In at ${checkInTime}` : 'Last: —'}
     </p>
     <p className="text-[9px] uppercase tracking-widest text-muted-foreground/60">
       Today: {isCheckedIn ? getTodayHours() : '0h 0m'}
     </p>
   </div>
   ```

   This removes the large clock and date, keeping just the status badge and time info in a clean horizontal layout.

6. Remove the `getTodayHours` function too since we're simplifying — actually keep it since it's used for "Today: Xh Ym". But simplify: remove the `formatTime` and `formatDate` functions only.

   Actually let me re-check: `getTodayHours` uses `currentTime` via the prop. So we need to keep `currentTime` prop or change approach.

   REVISED APPROACH: Keep `currentTime` prop (needed for `getTodayHours`), but remove only the visual clock/date display. Keep the status badge and last action/today hours on the right.

   So only remove:
   - The `<p>` with `font-serif text-4xl` (clock display)
   - The `<p>` with `text-[10px] uppercase tracking-[0.2em]` (date display)
   - The `formatTime` and `formatDate` functions

   Keep `currentTime` prop because `getTodayHours()` needs it.

   FINAL layout after removal — replace the entire header div (px-6 pt-6 pb-4 flex items-start justify-between) with:

   ```tsx
   <div className="px-6 pt-6 pb-4 flex items-center justify-between">
     <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest ${
       isCheckedIn
         ? 'bg-success-color/10 text-success-color'
         : 'bg-muted/20 text-muted-foreground'
     }`}
   >
     <span
       className={`w-2 h-2 rounded-full ${
         isCheckedIn ? 'bg-success-color' : 'bg-muted-foreground'
       }`}
     />
     {isCheckedIn ? 'Active' : 'Off Duty'}
   </div>

   <div className="text-right space-y-1">
     <p className="text-[9px] uppercase tracking-widest text-muted-foreground font-bold">
       {isCheckedIn && checkInTime ? `In at ${checkInTime}` : 'Last: —'}
     </p>
     <p className="text-[9px] uppercase tracking-widest text-muted-foreground/60">
       Today: {isCheckedIn ? getTodayHours() : '0h 0m'}
     </p>
   </div>
   ```

   And remove:
   - `formatTime` function
   - `formatDate` function
   - The clock `<p>` (font-serif text-4xl)
   - The date `<p>` (text-[10px] uppercase)
</action>

<acceptance_criteria>
- `frontend/src/components/SliderCheckIn.tsx` no longer has the `formatTime` function
- `frontend/src/components/SliderCheckIn.tsx` no longer has the `formatDate` function
- No clock display (font-serif text-4xl) in the component
- No date display (text-[10px] uppercase tracking-[0.2em]) in the component
- Status badge (Active/Off Duty with colored dot) is still displayed on the left
- Last action time and "Today: Xh Ym" are displayed on the right
- `currentTime` prop is kept (needed for getTodayHours)
- `getTodayHours()` function is kept and working
- `frontend/src/pages/StaffDashboard.tsx` still passes `currentTime={currentTime}` to SliderCheckIn (no change needed there)
- TypeScript check passes: `cd frontend && npx tsc --noEmit --skipLibCheck`
</acceptance_criteria>
