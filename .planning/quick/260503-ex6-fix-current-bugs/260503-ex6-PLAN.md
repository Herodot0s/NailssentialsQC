---
phase: quick
plan: 260503-ex6
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/middleware/authMiddleware.ts
  - backend/src/controllers/customerController.ts
autonomous: true
requirements: [quick-260503-ex6]
must_haves:
  truths:
    - Missing auth token returns 401 with TOKEN_REQUIRED error code
    - Invalid/expired auth token returns 401 with INVALID_TOKEN error code
    - Customer profile endpoints return 401 if user ID is missing from token
  artifacts:
    - path: backend/src/middleware/authMiddleware.ts
      provides: Correct auth error codes and HTTP status
      contains: "TOKEN_REQUIRED"
    - path: backend/src/controllers/customerController.ts
      provides: Null check for user ID in token
      contains: "userId"
  key_links:
    - from: backend/src/middleware/authMiddleware.ts
      to: backend/src/utils/jwt.ts
      via: verifyAccessToken call
      pattern: "verifyAccessToken"
---

<objective>
Fix current authentication and customer profile bugs in the NailssentialsQC system.

Purpose: Correct HTTP status codes and error codes in auth middleware to align with project conventions, and fix null user ID handling in customer controllers to prevent runtime errors.
Output: Updated auth middleware and customer controller with bug fixes applied.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/CLAUDE.md (Error Handling section for correct error codes)
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/middleware/authMiddleware.ts
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/controllers/customerController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth middleware error codes and HTTP status</name>
  <files>backend/src/middleware/authMiddleware.ts</files>
  <action>
    Fix two issues in authMiddleware.ts per CLAUDE.md error code conventions:
    1. Line 24: Change error code from 'ACCESS_DENIED' to 'TOKEN_REQUIRED' for missing token case (per CLAUDE.md: TOKEN_REQUIRED = Missing token).
    2. Lines 33-36: Change invalid/expired token response from 403 to 401 (authentication failure, not authorization). The error code 'INVALID_TOKEN' is correct per CLAUDE.md, only the HTTP status needs updating.
    
    Per D-00 (project convention): All auth errors must use correct HTTP status codes (401 for auth failures, 403 for authorization failures) and matching error codes from CLAUDE.md.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/appointments -i (expect 401 with TOKEN_REQUIRED); curl -X GET http://localhost:3000/api/appointments -H "Authorization: Bearer invalid-token" -i (expect 401 with INVALID_TOKEN)</automated>
  </verify>
  <done>Missing token returns 401 with TOKEN_REQUIRED code; invalid token returns 401 with INVALID_TOKEN code</done>
</task>

<task type="auto">
  <name>Task 2: Fix customer controller null user ID handling</name>
  <files>backend/src/controllers/customerController.ts</files>
  <action>
    Add null check for req.user?.sub in getMyProfile and updateMyProfile functions to prevent Prisma query errors when user ID is missing:
    1. In getMyProfile (line 7): After const userId = req.user?.sub as number | undefined;, add:
       if (!userId) {
         return res.status(401).json({ success: false, error: { code: 'TOKEN_REQUIRED', message: 'Invalid user token' } });
       }
    2. In updateMyProfile (line 25): Same as above, add null check for userId after extraction.
    
    Per D-00 (project convention): All controller functions must validate required user context before database operations.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/customers/profile -H "Authorization: Bearer valid-token-for-test-user" (expect 200); Modify token to have invalid sub, expect 401 with TOKEN_REQUIRED</automated>
  </verify>
  <done>getMyProfile and updateMyProfile return 401 with TOKEN_REQUIRED if userId is missing from token</done>
</task>

</tasks>

<verification>
1. Auth middleware returns correct status codes and error codes for missing/invalid tokens
2. Customer profile endpoints handle missing user ID gracefully without Prisma errors
3. Existing functionality remains unchanged (test with valid tokens)
</verification>

<success_criteria>
- Auth middleware missing token: 401 status, TOKEN_REQUIRED code
- Auth middleware invalid token: 401 status, INVALID_TOKEN code
- Customer getMyProfile with missing userId: 401 status, TOKEN_REQUIRED code
- Customer updateMyProfile with missing userId: 401 status, TOKEN_REQUIRED code
- All existing passing endpoints remain functional
</success_criteria>
