---
phase: 260503-fq0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/src/utils/jwt.ts", "backend/src/middleware/authMiddleware.ts"]
autonomous: true
requirements: ["SEC-01", "DEBT-01"]
user_setup: []
must_haves:
  truths:
    - "Server fails to start if JWT_SECRET or REFRESH_TOKEN_SECRET are missing (existing fail-fast behavior preserved)"
    - "JWT access tokens have type: 'access' and refresh tokens have type: 'refresh' in payload"
    - "verifyAccessToken only accepts tokens with type: 'access', verifyRefreshToken only accepts type: 'refresh'"
    - "Verify functions return AppJwtPayload (typed with sub, email, role, type) instead of generic JwtPayload"
    - "Invalid/expired tokens throw custom typed errors (TokenExpiredError, InvalidTokenError, TokenTypeError)"
    - "getTokenExpiration utility can extract expiration date from a token without verification"
    - "authMiddleware.ts imports AppJwtPayload from jwt.ts instead of defining it locally"
  artifacts:
    - path: "backend/src/utils/jwt.ts"
      provides: "JWT utility with typed payloads, error handling, token type discrimination, and expiration utility"
      contains: ["AppJwtPayload", "generateAccessToken", "verifyAccessToken", "getTokenExpiration"]
    - path: "backend/src/middleware/authMiddleware.ts"
      provides: "Auth middleware using updated jwt.ts types"
      contains: ["AuthRequest", "authenticateToken", "authorizeRoles"]
  key_links:
    - from: "backend/src/middleware/authMiddleware.ts"
      to: "backend/src/utils/jwt.ts"
      via: "import { verifyAccessToken, AppJwtPayload } from '../utils/jwt'"
      pattern: "import.*from.*jwt"
---

<objective>
Improve the JWT utility (jwt.ts) with proper TypeScript interfaces, error handling, token type discrimination, and utilities. Update authMiddleware.ts to use shared types from jwt.ts, maintaining backward compatibility.

Purpose: Make JWT operations more secure (prevent token type mixing), type-safe (proper interfaces), and maintainable (custom error types, utility functions). Preserve existing fail-fast startup behavior for missing JWT secrets.

Output: Updated backend/src/utils/jwt.ts and backend/src/middleware/authMiddleware.ts with all improvements.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/PROJECT.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/ROADMAP.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/.planning/STATE.md
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/utils/jwt.ts
@C:/Users/Administrator/Desktop/nailssentialsqc-system/backend/src/middleware/authMiddleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor jwt.ts with types, error handling, and token type discrimination</name>
  <files>backend/src/utils/jwt.ts</files>
  <action>
    1. Preserve existing fail-fast startup checks for JWT_SECRET and REFRESH_TOKEN_SECRET (lines 6-14 in current jwt.ts) — do not modify or remove these.
    2. Define AppJwtPayload interface with fields: sub (string | number), email (string), role (string), type ('access' | 'refresh').
    3. Define custom error classes extending Error:
       - JwtError (base class with code field)
       - TokenExpiredError (code: 'TOKEN_EXPIRED')
       - InvalidTokenError (code: 'INVALID_TOKEN')
       - TokenTypeError (code: 'TOKEN_TYPE_MISMATCH', expectedType: string, actualType: string)
    4. Update generateAccessToken to spread the payload with { type: 'access' } before signing. Update generateRefreshToken similarly with { type: 'refresh' }.
    5. Refactor verifyAccessToken:
       - Wrap jwt.verify in try/catch
       - On error: if error is jsonwebtoken.TokenExpiredError, throw TokenExpiredError; if jsonwebtoken.JsonWebTokenError, throw InvalidTokenError; else rethrow.
       - After verification, check that payload.type === 'access', else throw TokenTypeError('access', payload.type).
       - Return payload as AppJwtPayload.
    6. Refactor verifyRefreshToken similarly, checking for type === 'refresh'.
    7. Add utility function getTokenExpiration(token: string): Date | null that uses jwt.decode (no verification) to get the exp claim, converts to Date, returns null if invalid.
    8. Export all types (AppJwtPayload, JwtError, TokenExpiredError, etc.) and functions.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/utils/jwt.ts</automated>
  </verify>
  <done>jwt.ts has proper types, error handling, token type checks, and getTokenExpiration utility. Fail-fast checks preserved. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update authMiddleware.ts to use types from jwt.ts</name>
  <files>backend/src/middleware/authMiddleware.ts</files>
  <action>
    1. Remove the local AppJwtPayload interface definition (lines 5-9 in current authMiddleware.ts).
    2. Import AppJwtPayload from '../utils/jwt'.
    3. In authenticateToken, remove the 'as unknown as AppJwtPayload' cast on verifyAccessToken call, since verifyAccessToken now returns AppJwtPayload directly.
    4. Ensure error handling in authenticateToken still catches errors from verifyAccessToken and returns 401 with INVALID_TOKEN code.
    5. Verify that AuthRequest interface still correctly extends Request with user?: AppJwtPayload.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/middleware/authMiddleware.ts</automated>
  </verify>
  <done>authMiddleware.ts imports AppJwtPayload from jwt.ts, no local definition. TypeScript compiles without errors. Backward compatible (authenticateToken still works as before).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted JWT tokens sent in Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-01 | Spoofing | jwt.ts verify functions | mitigate | Add token type discrimination (type field in payload, checked on verify) to prevent using refresh tokens as access tokens |
| T-260503-02 | Tampering | jwt.ts verify functions | mitigate | jwt.verify already validates signature using secret; no changes needed |
| T-260503-03 | Information Disclosure | jwt.ts | accept | JWT secrets stored in env vars, not in code; existing fail-fast checks prevent startup without secrets |
| T-260503-04 | Denial of Service | auth endpoints | accept | Rate limiting already implemented in Phase 3 Plan 03-04; not in scope here |
| T-260503-05 | Elevation of Privilege | jwt.ts verify functions | mitigate | Token type check prevents using a refresh token (lower privilege) as an access token |
</threat_model>

<verification>
1. TypeScript compilation passes for both modified files: `cd backend && npx tsc --noEmit`
2. Existing fail-fast behavior: temporarily rename JWT_SECRET in .env, run `cd backend && npm run dev` → should throw error and exit.
3. Token type check: generate a refresh token, try to verify it with verifyAccessToken → should throw TokenTypeError.
4. Error handling: verify an expired/invalid token → should throw custom TokenExpiredError/InvalidTokenError.
</verification>

<success_criteria>
1. backend/src/utils/jwt.ts has AppJwtPayload interface, custom error classes, token type discrimination, getTokenExpiration utility.
2. Fail-fast startup checks for JWT secrets are preserved.
3. backend/src/middleware/authMiddleware.ts imports AppJwtPayload from jwt.ts, no local definition.
4. TypeScript compilation passes for backend with no errors.
5. All existing functionality (authenticateToken, authorizeRoles) works as before (backward compatible).
</success_criteria>

<output>
After completion, create `.planning/quick/260503-fq0-jwt-secret-environment-variable-is-requi/260503-fq0-SUMMARY.md`
</output>
